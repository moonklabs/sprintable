# ⓒ 라이브 env 드리프트 가드 — 카테고리 C(순수 수동) 허용목록
#
# 설계 doc: env-drift-guard-design(26c673c2-40be-4fd7-98eb-47e562513ab9)
#
# 이 파일의 역할: `gcloud run services list`로 동적 열거된 라이브 Cloud Run 서비스의 env var가
# IaC(cloudbuild.yaml/.github/workflows/cloud-build.yml)나 레거시 부트스트랩 스크립트
# (deploy_backend.sh/deploy_frontend.sh/deploy_mcp_*.sh)의 어느 쪽에도 선언돼 있지 않을 때,
# "누락"이 아니라 "알고 있고 승인된 수동 값"임을 명시하는 유일한 인정 경로.
# 여기 없는 라이브 키는 가드가 FAIL로 잡는다 — 실렵된 게 아니라 "파이프라인에 편입 필요"
# 아니면 "여기 등재 필요" 둘 중 하나로 처리한다(오탐 없음이 설계 전제).
#
# 2026-07-22 기준 실측(gcloud run services describe 전 서비스) 1:1 대조로 작성 — 기억이 아닌
# 라이브 상태 그대로. 값 자체는 여기 안 옮긴다(키 이름·서비스·사유만 — 시크릿이든 아니든).
#
# ⚠️ ③(평문 시크릿 형태 검출)의 한계 — 현재 `sk_live_` 프리픽스 형태만 검출한다
# (check_env_drift.py의 `_SECRET_SHAPE_RE`). `sk_test_`·GitHub 토큰·GCP 키 등 다른 시크릿
# 형태는 이 패턴으로 못 잡는다 — 스코프 확대는 후속(오르테가군 지적: 못 잡는 것을 못
# 잡는다고 적어두는 것이 오늘 계속 해온 방식).

# 이 guard의 스코프 밖 서비스 — "전부 제외"가 아니라 "축별 제외"(오르테가군 지적,
# 2026-07-22: mcp-dev 사고가 정확히 "아무도 그 서비스를 안 봐서" 났다 — 제외가 감시 밖이
# 되면 이 가드를 만든 이유 자체가 무효화된다). ①키집합·②값대조는 이 repo에 IaC가 없어
# 대조 기준이 없으니 오탐 확定(제외 정당) — 그러나 ③평문 시크릿 형태 검출은 IaC 여부와
# 무관하게 라이브 env value만 보면 판정되는 축이라 절대 빼지 않는다(check_env_drift.py가
# excluded_services 여부와 무관하게 전 서비스에 ③을 적용 — 코드 참고).
#
# `watched_by`: "우리 소관 아니다"와 "아무도 안 본다"는 다른 것 — 저쪽에 대응 가드가
# 있으면 그 근거를, 없으면 없다는 사실 자체를 남긴다(다음 사람이 "저쪽에서 보겠지"로
# 잘못 안심하지 않도록).
excluded_services:
  - service: sprintable-admin-web
    excluded_axes: [key_set, value_check]
    reason: >-
      IaC가 별도 repo(sprintable-admin, private)에 있음 — 이 repo(sprintable)의
      cloudbuild.yaml/deploy_*.sh가 관장하지 않는 서비스라 ①②축 대조 기준이 없음.
    watched_by: >-
      2026-07-22 sprintable-admin repo GHA 워크플로우(internal-api-ci.yml/web-ci.yml)
      전수 확認 — env-var/시크릿 드리프트 가드 없음. `internal-api/app/core/drift.py`의
      "Drift-check"는 DB 스키마(vendored model contract ↔ live information_schema)
      drift로 이것과 무관. ⛔ ①②축은 어느 쪽에서도 현재 안 보는 상태 — 저쪽 repo에
      별도 가드 신설이 후속 필요(백로그, story #2099). ③축(평문 시크릿)은 이 가드가 계속
      커버. 갭 경위·drift.py 오판 위험·AC5("이름이 아니라 무엇을 검사하는지로 판정")는
      #2099 참고.
  - service: sprintable-admin-web-dev
    excluded_axes: [key_set, value_check]
    reason: 위와 동일 — sprintable-admin repo 소관.
    watched_by: 위와 동일 — ①②축 무주공산(story #2099), ③축은 이 가드가 커버.
  - service: sprintable-internal-api
    excluded_axes: [key_set, value_check]
    reason: 위와 동일 — sprintable-admin repo 소관(E-ADMIN internal-api).
    watched_by: 위와 동일 — ①②축 무주공산(story #2099), ③축은 이 가드가 커버.
  - service: sprintable-internal-api-dev
    excluded_axes: [key_set, value_check]
    reason: 위와 동일 — sprintable-admin repo 소관.
    watched_by: 위와 동일 — ①②축 무주공산(story #2099), ③축은 이 가드가 커버.

services:
  sprintable-backend-dev:
    keys:
      - key: BUILD_APP_METADATA_DEFALLBACK
        value_check: skip
        reason: 908075db de-fallback 단계 플래그(단계4 미착수) — 수동 토글, IaC 편입 시기 아직 아님.
        owner: TBD
      - key: CRON_SECRET
        reason: cron 엔드포인트 인증 시크릿 — 부트스트랩 스크립트 미편입, 수동 시크릿.
        owner: TBD
      - key: DATABASE_URL_DEV
        reason: dev DB 직결 폴백 변수로 추정 — 용도/생성 경위 미확認, owner 확인 필요.
        owner: TBD
      - key: DECISION_GATE_LINE_ENABLED
        value_check: skip
        reason: feature flag(org allowlist 게이팅류) — 수동 롤아웃 토글.
        owner: TBD
      - key: DECISION_GATE_LINE_MODE
        value_check: skip
        reason: 위 플래그 계열 — 수동 값.
        owner: TBD
      - key: DECISION_GATE_LINE_ORG_ALLOWLIST
        reason: org allowlist 값 자체 — 수동 편집·PR 리뷰 아래 있지 않음.
        owner: TBD
      - key: FIREBASE_BFF_INTERNAL_SECRET
        reason: Firebase BFF 내부 시크릿 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: FIREBASE_OAUTH_HANDOFF_ENABLED
        value_check: skip
        reason: feature flag — 수동 토글.
        owner: TBD
      - key: GATE_CONFIG_ENFORCE_ENABLED
        value_check: skip
        reason: feature flag(게이트 강제) — 수동 토글.
        owner: TBD
      - key: GATE_CONFIG_ENFORCE_ORG_ALLOWLIST
        reason: org allowlist 값 — 수동 편집.
        owner: TBD
      - key: GITHUB_APP_CLIENT_ID
        reason: GitHub App 자격증명 — 부트스트랩 스크립트 미편입, 수동 시크릿 계열.
        owner: TBD
      - key: GITHUB_APP_CLIENT_SECRET
        reason: 위와 동일.
        owner: TBD
      - key: GITHUB_APP_ID
        reason: 위와 동일.
        owner: TBD
      - key: GITHUB_APP_PRIVATE_KEY
        reason: >-
          위와 동일 — github_app.py:59,63의 app_env!="production" 비교가 prod 라이브
          APP_ENV="prod"와 불일치(별건 기존 결함, project_ci_gate_not_enforced류 트래킹 참고).
        owner: TBD
      - key: GITHUB_APP_SLUG
        reason: GitHub App 메타 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: GITHUB_APP_STATE_SECRET
        reason: GitHub App OAuth state 서명 시크릿 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: GITHUB_WEBHOOK_SECRET
        reason: GitHub 웹훅 서명 검증 시크릿 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: GITHUB_APP_WEBHOOK_SECRET
        reason: >-
          GitHub App 웹훅 서명 검증 — 이 값이 없으면 App 서명 검증을 통째로 건너뛰고
          «전부 legacy» 로 떨어진다(#2327 실측, app_inert = not app_secret). 부트스트랩
          스크립트 미편입(GITHUB_WEBHOOK_SECRET과 동일 카테고리) — 값은 GitHub App
          설정 화면에서만 얻을 수 있어 별도 확認 필요, 이 항목은 그 값이 채워지기 前에도
          허용목록 등재만 먼저 한다.
        owner: TBD
      - key: H1_MERGE_GATE_ADVISORY
        value_check: skip
        reason: feature flag — 수동 토글.
        owner: TBD
      - key: H1_MERGE_GATE_ENABLED
        value_check: skip
        reason: feature flag — 수동 토글.
        owner: TBD
      - key: H1_MERGE_GATE_ORG_ALLOWLIST
        reason: org allowlist 값 — 수동 편집.
        owner: TBD
      - key: L2_TRIGGER_ADVISORY_LOCK
        value_check: skip
        reason: feature flag — 수동 토글.
        owner: TBD
      - key: L2_TRIGGER_ENABLED
        value_check: skip
        reason: feature flag — 수동 토글.
        owner: TBD
      - key: L2_TRIGGER_MAX_WAKES_PER_ORG_PER_HOUR
        reason: 레이트리밋 수치 — 수동 튜닝값.
        owner: TBD
      - key: L2_TRIGGER_ORG_ALLOWLIST
        reason: org allowlist 값 — 수동 편집.
        owner: TBD
      - key: LICENSE_CONSENT
        value_check: skip
        reason: 라이선스 동의 플래그 — 수동 1회성 값으로 추정.
        owner: TBD
      - key: LLM_GEMINI_LOCATION
        reason: Gemini 리전 설정 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: LLM_GEMINI_MODEL
        reason: Gemini 모델 지정 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: MCP_PUBLIC_URL
        reason: MCP 공개 URL — 부트스트랩 스크립트 미편입, 서비스 URL 확정 후 1회성 수동 설정으로 추정.
        owner: TBD
      - key: OPS_RESTART_TS
        value_check: skip
        reason: >-
          "값 자체가 무의미·재시작 트리거용 더미"로 추정(키 이름상) — 값 비교 자체가 무의미해
          skip. 존재 여부만 추적.
        owner: TBD
      - key: ORG_BILLING_KEY_ENCRYPTION_KEY
        reason: >-
          결제②-C1(#2492) MultiFernet 암호화 키 — org_billing_keys.encrypted_billing_key
          봉인용(billing_key_crypto.py). Secret Manager 등록·Cloud Run env 배선 PO 수동
          프로비저닝(2026-08-06, C1 착수 GO 메시지) — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: STORAGE_PROVIDER
        value_check: skip
        reason: 스토리지 프로바이더 선택 플래그 — 수동 토글.
        owner: TBD
      - key: TOSS_PAYMENTS_CLIENT_KEY
        reason: >-
          결제②-C0(#2491) Toss Payments 클라이언트 키 — state/shared/credentials/
          toss-payments-keys.md(dev=test_ck)에 이미 발급돼 있던 값. app/core/config.py
          Settings.toss_payments_client_key로 읽음. Secret Manager 등록 PO 수동
          프로비저닝(2026-08-06) — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: TOSS_PAYMENTS_CRYPTO_KEY
        reason: dev와 동일 사유(TOSS_PAYMENTS_CLIENT_KEY 참고) — test_crypto 값, Settings.toss_payments_crypto_key.
        owner: TBD
      - key: TOSS_PAYMENTS_SECRET_KEY
        reason: dev와 동일 사유(TOSS_PAYMENTS_CLIENT_KEY 참고) — test_sk 값, Settings.toss_payments_secret_key.
        owner: TBD
      - key: TOSS_WEBHOOK_SECRET
        reason: >-
          #2728 Toss 웹훅 서명 검증용 시크릿 — Secret Manager(TOSS_WEBHOOK_SECRET_DEV)에
          이미 발급돼 있던 값. app/core/config.py Settings.toss_webhook_secret로 읽음.
          Secret Manager 등록·Cloud Run env 수동 배선 PO(#2728 착수 時) — TOSS_PAYMENTS_*
          3건과 동형, 등재만 누락돼 있었음(story #2739/054edff4).
        owner: TBD

  sprintable-backend-prod:
    keys:
      - key: CRON_SECRET
        reason: dev와 동일 — cron 엔드포인트 인증 시크릿, 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: FIREBASE_OAUTH_HANDOFF_ENABLED
        value_check: skip
        reason: >-
          story #1931 feature flag(dev/realtime-dev와 동일 축) — settings.py:261
          firebase_oauth_handoff_enabled, backend/app/routers/auth_firebase_internal.py:503,576
          이 게이트로 issue/consume 501 분기(껐다 켰다 배포 없이 토글하는 것이 이 값의
          존재 이유라 SSOT 편입 대상 아님). frontend-prod의 동일 키와 «짝»이라 — 프론트가
          FIREBASE_OAUTH_HANDOFF_ENABLED를 켠 채 이 값만 꺼지면 BFF 내부호출이 501로
          막힌다. ⛔만료 조건: handoff가 기본 동작으로 승격되면 이 값·위 게이트·FE 분기를
          함께 제거할 것(2026-07-28 인시던트 대응 중 수동 세팅 확認, 계정으로 주체 특定 불가).
          ⚠️값 형식 위험: 지금 이 서비스는 `1`(pydantic bool 파서가 True로 읽음), frontend-prod는
          문자열 `true`(정확 일치 비교) — 지금은 둘 다 켜진 뜻이 같지만, 누가 frontend를
          `1`로 바꾸면 그쪽은 정확 일치가 깨져 조용히 꺼진다. 통일은 이번 판 스코프 밖 — 위험만
          기록해 둔다.
        owner: TBD
      - key: GITHUB_APP_CLIENT_ID
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_CLIENT_SECRET
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_ID
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_PRIVATE_KEY
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_SLUG
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_STATE_SECRET
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_WEBHOOK_SECRET
        reason: dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_WEBHOOK_SECRET
        reason: dev와 동일 사유 — GitHub App 웹훅 서명 검증(#2327 실측 참고).
        owner: TBD
      - key: H1_MERGE_GATE_ADVISORY
        value_check: skip
        reason: dev와 동일 — feature flag.
        owner: TBD
      - key: H1_MERGE_GATE_ENABLED
        value_check: skip
        reason: dev와 동일 — feature flag.
        owner: TBD
      - key: H1_MERGE_GATE_ORG_ALLOWLIST
        reason: dev와 동일 — org allowlist 값.
        owner: TBD
      - key: LICENSE_CONSENT
        value_check: skip
        reason: dev와 동일.
        owner: TBD
      - key: MCP_PUBLIC_URL
        reason: dev와 동일 사유.
        owner: TBD
      - key: STORAGE_PROVIDER
        value_check: skip
        reason: dev와 동일.
        owner: TBD

  sprintable-realtime-dev:
    keys:
      # cloudbuild.yaml deploy-realtime 스텝이 FASTAPI_URL/DB_POOL_SIZE/DB_MAX_OVERFLOW/
      # PG_LISTEN_ENABLED/REDIS_CONSUME_ENABLED/REDIS_URL/EVENT_BROKER_REDIS_DUAL_PUBLISH_
      # ENABLED/EVENT_BROKER_REDIS_DISPATCH_ENABLED를 명시 관리 — 그 외 전부는 backend와 동일
      # 이미지를 쓰며 "--set-secrets 없는 gcloud run deploy 컨벤션(기존 바인딩 preserve)"으로
      # 최초 생성 시(story #2078) 1회성 수동 바인딩된 것으로 추정 — backend-dev와 동일 키 집합.
      - key: BUILD_APP_METADATA_DEFALLBACK
        value_check: skip
        reason: backend-dev와 동일 사유(같은 이미지·같은 플래그).
        owner: TBD
      - key: CRON_SECRET
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: DATABASE_URL_DEV
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: DECISION_GATE_LINE_ENABLED
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: DECISION_GATE_LINE_MODE
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: DECISION_GATE_LINE_ORG_ALLOWLIST
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: FIREBASE_BFF_INTERNAL_SECRET
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: FIREBASE_OAUTH_HANDOFF_ENABLED
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GATE_CONFIG_ENFORCE_ENABLED
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GATE_CONFIG_ENFORCE_ORG_ALLOWLIST
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_CLIENT_ID
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_CLIENT_SECRET
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_ID
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_PRIVATE_KEY
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_SLUG
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_STATE_SECRET
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_WEBHOOK_SECRET
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: GITHUB_APP_WEBHOOK_SECRET
        reason: backend-dev와 동일 사유 — GitHub App 웹훅 서명 검증(#2327 실측 참고).
        owner: TBD
      - key: H1_MERGE_GATE_ADVISORY
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: H1_MERGE_GATE_ENABLED
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: H1_MERGE_GATE_ORG_ALLOWLIST
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: L2_TRIGGER_ADVISORY_LOCK
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: L2_TRIGGER_ENABLED
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: L2_TRIGGER_MAX_WAKES_PER_ORG_PER_HOUR
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: L2_TRIGGER_ORG_ALLOWLIST
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: LICENSE_CONSENT
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: LLM_GEMINI_LOCATION
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: LLM_GEMINI_MODEL
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: MCP_PUBLIC_URL
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: OPS_RESTART_TS
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD
      - key: STORAGE_PROVIDER
        value_check: skip
        reason: backend-dev와 동일 사유.
        owner: TBD

  sprintable-frontend-dev:
    keys:
      - key: NEXT_PUBLIC_APP_URL
        reason: 부트스트랩 스크립트 미편입 — 용도/생성 경위 확인 필요.
        owner: TBD
      - key: FIREBASE_BFF_INTERNAL_SECRET
        reason: BFF↔Firebase 내부 시크릿 — 부트스트랩 스크립트 미편입.
        owner: TBD
      - key: FIREBASE_OAUTH_HANDOFF_ENABLED
        value_check: skip
        reason: feature flag — 수동 토글.
        owner: TBD
      - key: STORAGE_PROVIDER
        value_check: skip
        reason: 스토리지 프로바이더 선택 플래그 — 수동 토글.
        owner: TBD

  sprintable-frontend-prod:
    keys:
      - key: APP_BASE_URL
        reason: 용도/생성 경위 미확認 — owner 확인 필요(TBD로 이미 알려진 항목, memory 참고).
        owner: TBD
      - key: FIREBASE_OAUTH_HANDOFF_ENABLED
        value_check: skip
        reason: >-
          story #1931 feature flag(dev와 동일 축) — apps/web/src/app/auth/oauth-handoff/route.ts:65
          가 이 값을 정확히 문자열 'true'로 검사한다(껐다 켰다 배포 없이 토글하는 것이 이
          값의 존재 이유라 SSOT 편입 대상 아님). backend-prod의 동일 키와 «짝»이라 — 둘이
          같이 켜져 있어야 handoff issue/consume 내부호출이 성공한다. ⛔만료 조건: handoff가
          기본 동작으로 승격되면 이 값·FE 분기·백엔드 게이트를 함께 제거할 것(2026-07-28
          인시던트 대응 중 수동 세팅 확認, 계정으로 주체 특定 불가).
          ⚠️값 형식 위험: 이 서비스는 문자열 `true`(정확 일치 비교), backend-prod는 `1`(pydantic
          bool 파서가 True로 읽음) — 지금은 둘 다 켜진 뜻이 같지만, 이 값이 `1`로 바뀌면 route.ts:65의
          정확 일치가 깨져 프론트만 조용히 꺼진다. 통일은 이번 판 스코프 밖 — 위험만 기록해 둔다.
        owner: TBD
      - key: SECRET_REFRESH_1777617854
        reason: >-
          키 이름 자체가 타임스탬프 기반 시크릿 회전 흔적으로 추정 — 용도 미확認,
          owner 확인 필요.
        owner: TBD
      - key: STORAGE_PROVIDER
        value_check: skip
        reason: dev와 동일 — 스토리지 프로바이더 선택 플래그.
        owner: TBD
      - key: SUPABASE_SERVICE_ROLE_KEY
        reason: Supabase 서비스 롤 키 — 부트스트랩 스크립트 미편입, prod-only.
        owner: TBD
        owner: TBD

  # sprintable-mcp-dev / sprintable-mcp-prod: 실측 결과 라이브 키 전부(MCP_TRANSPORT·
  # SPRINTABLE_API_URL·AGENT_API_KEY·[prod만]MCP_ALLOWED_HOSTS) deploy_mcp_dev.sh/
  # deploy_mcp_prod.sh가 전량 관리 — 카테고리 C 없음(entry 불필요, 완전 커버).

# story #2135(2026-07-24) 축④ "Settings 커버리지" 전용 — app.core.config.Settings가 이
# 키를 모르는 게 정상인 경우(다른 메커니즘으로 읽거나 의도적으로 안 읽음). 위 services.*.keys
# (①②축, "IaC에 선언 안 됨"의 사유)와는 다른 질문("Settings 필드명과 안 맞음"의 사유)이라
# 별도 섹션 — 오르테가 라이브 실측(backend-dev/prod, Cloud Run describe spec, 2026-07-24)
# 기준 전수 triage. ⚠️사유는 "os.getenv 직접"으로 뭉뚱그리지 않고 실제로 읽는 파일을 명시한다
# — 그 파일이 나중에 지워지는데 이 entry만 남으면 다시 조용해지는 문제를 막기 위함(오르테가
# 지적). 이 목록에 없고 Settings 필드도 아닌 라이브 키는 축④가 FAIL로 잡는다.
settings_exempt:
  - key: CRON_SECRET
    reason: >-
      app/routers/cron.py:34 `os.environ.get("CRON_SECRET")` — Settings 필드가 아니라
      라우터가 직접 읽음(cron 엔드포인트 인증).
  - key: EMAIL_FROM
    reason: app/services/email.py:37,47 `os.getenv("EMAIL_FROM", ...)` 직접 읽음.
  - key: RESEND_API_KEY
    reason: app/services/email.py:19 `os.getenv("RESEND_API_KEY", "")` 직접 읽음.
  - key: STORAGE_PROVIDER
    reason: app/services/storage/factory.py:17 `os.environ.get("STORAGE_PROVIDER", ...)` 직접 읽음.
  - key: NEXT_PUBLIC_APP_URL
    reason: >-
      app/routers/docs.py:538·app/routers/auth.py:601,1134 등 `os.environ.get(
      "NEXT_PUBLIC_APP_URL", ...)` 직접 읽음(프론트 관례 이름을 백엔드도 공유).
  - key: LLM_GEMINI_LOCATION
    reason: >-
      app/services/llm_client.py:48 `os.environ.get("LLM_GEMINI_LOCATION") or
      settings.vertex_ai_location` — dev 전용 override, os.environ 직접 우선 읽음.
  - key: LLM_GEMINI_MODEL
    reason: >-
      app/services/llm_client.py:43 `os.environ.get("LLM_GEMINI_MODEL", "gemini-2.5-flash")`
      직접 읽음 — dev 전용 override.
  - key: FASTAPI_URL
    reason: >-
      app/services/agent_onboarding_config.py:236,248 `_BACKEND_URL_ENV_KEY="FASTAPI_URL"` →
      `os.environ.get(_BACKEND_URL_ENV_KEY, ...)` 간접 참조(상수를 거쳐 읽음 — 리터럴
      grep으로는 안 잡힘, 이 사유를 지우려면 그 상수 정의도 같이 사라져야 함).
  - key: MCP_PUBLIC_URL
    reason: >-
      app/services/agent_onboarding_config.py:258 `os.environ.get(_MCP_PUBLIC_URL_ENV_KEY,
      "")` — backend app Settings 대상 아님(sprintable_mcp는 별도 런타임/이미지).
  - key: OPS_RESTART_TS
    reason: >-
      backend/scripts/deploy_realtime_gce.sh:349 "의도적 제외" — 재시작 트리거용 타임스탬프
      마커, 코드가 읽는 값이 아니라 애초에 no-read by design.

# ⑤(story #2296, 2026-07-28) — apps/web이 읽는데 IaC∪라이브 어디에도 없는 키 중, 코드로
# 직접 확認해 "정상(조건부로만 필요·기본값으로 도는 게 맞음)"이라고 판단한 것들.
# ⛔여기 등재는 "영원히 안 본다"가 아니라 "지금 근거를 남긴다"다 — 그 조건부 스위치가
# 실제로 켜지는 환경이 생기면 이 사유도 같이 재검토해야 한다.
code_read_exempt:
  - key: KMS_PROVIDER
    reason: >-
      apps/web/src/lib/kms/provider.ts — 'local'|'gcp'|'vault' 스위치, 기본값 'local'로
      떨어지면 아래 GCP_KMS_*/VAULT_* 자체가 안 읽힘(코드 확認).
  - key: LOCAL_KMS_MASTER_KEY
    reason: KMS_PROVIDER=local(기본값)일 때만 실제로 쓰임 — 위 항목과 같은 스위치.
  - key: GCP_KMS_BEARER_TOKEN
    reason: KMS_PROVIDER=gcp일 때만 쓰임(기본은 local) — provider.ts 코드 확認.
  - key: GCP_KMS_KEY_NAME
    reason: KMS_PROVIDER=gcp일 때만 쓰임(기본은 local) — provider.ts 코드 확認.
  - key: VAULT_ADDR
    reason: KMS_PROVIDER=vault일 때만 쓰임(기본은 local) — provider.ts 코드 확認.
  - key: VAULT_TOKEN
    reason: KMS_PROVIDER=vault일 때만 쓰임(기본은 local) — provider.ts 코드 확認.
  - key: STORAGE_PROVIDER
    reason: >-
      apps/web/src/lib/storage/config.ts:12-13 — 기본값 'local'로 떨어짐(코드 확認, ㉢로도
      이미 낮음 등급). 아래 S3_*/GCS_*는 이 값이 s3/gcs일 때만 실제로 쓰임.
  - key: S3_ACCESS_KEY_ID
    reason: STORAGE_PROVIDER=s3일 때만 쓰임(기본은 local) — config.ts 코드 확認.
  - key: S3_ENDPOINT
    reason: STORAGE_PROVIDER=s3일 때만 쓰임(기본은 local) — config.ts 코드 확認.
  - key: S3_SECRET_ACCESS_KEY
    reason: STORAGE_PROVIDER=s3일 때만 쓰임(기본은 local) — config.ts 코드 확認.
  - key: GCS_CREDENTIALS_JSON
    reason: STORAGE_PROVIDER=gcs일 때만 쓰임(기본은 local) — providers/gcs.ts 코드 확認.
  - key: GCS_PROJECT_ID
    reason: STORAGE_PROVIDER=gcs일 때만 쓰임(기본은 local) — providers/gcs.ts 코드 확認.
  - key: INTERNAL_DOGFOOD_ACCESS_ENABLED
    reason: >-
      apps/web/src/lib/internal-dogfood.ts:44 `=== 'true'` — 미설정 시 false로 떨어지는
      명시적 기능 토글(내부 도그푸딩 전용, 이름 자체가 그 스코프를 명시).
  - key: INTERNAL_DOGFOOD_ACCESS_MAX_AGE_SECONDS
    reason: INTERNAL_DOGFOOD_ACCESS_ENABLED가 꺼져 있으면(기본) 실질 영향 없음 — 같은 기능군.
  - key: INTERNAL_DOGFOOD_ACCESS_SECRET
    reason: INTERNAL_DOGFOOD_ACCESS_ENABLED가 꺼져 있으면(기본) 실질 영향 없음 — 같은 기능군.
  - key: INTERNAL_DOGFOOD_DEFAULT_ORG_ID
    reason: INTERNAL_DOGFOOD_ACCESS_ENABLED가 꺼져 있으면(기본) 실질 영향 없음 — 같은 기능군.
  - key: INTERNAL_DOGFOOD_DEFAULT_PROJECT_ID
    reason: INTERNAL_DOGFOOD_ACCESS_ENABLED가 꺼져 있으면(기본) 실질 영향 없음 — 같은 기능군.
  - key: INTERNAL_DOGFOOD_DEFAULT_PROJECT_NAME
    reason: INTERNAL_DOGFOOD_ACCESS_ENABLED가 꺼져 있으면(기본) 실질 영향 없음 — 같은 기능군.
  - key: SPRINTABLE_RUNTIME_ROLE
    reason: >-
      apps/web/src/services/background-runtime.ts:82-88 `resolveBackgroundRuntimeRole` —
      미설정 시 `NODE_ENV==='production' ? 'web' : 'all'`로 떨어진다. frontend-dev/prod
      둘 다 NODE_ENV=production(gcloud로 직접 확認, 2026-08-02)이라 기본값은 'web' —
      instrumentation.ts의 `shouldStartBackgroundRuntime`이 role이 'worker'|'all'일 때만
      true이므로 미설정 상태에서 백그라운드 워커(discord/teams/memo 아웃바운드 디스패처)는
      항상 안 켜진다(story #2423, 페드루 판정 — 이 기능군은 은퇴 상태). 값을 채워야 도는
      기능이 아니라, 안 채워야 지금 의도한 대로(web-only) 도는 스위치.
  - key: SPRINTABLE_BACKGROUND_POLL_INTERVAL_MS
    reason: SPRINTABLE_RUNTIME_ROLE이 web으로 떨어지면(기본) 워커 자체가 안 켜져 실질 영향 없음 — 같은 기능군.
  - key: SPRINTABLE_DISCORD_OUTBOUND_POLL_INTERVAL_MS
    reason: SPRINTABLE_RUNTIME_ROLE이 web으로 떨어지면(기본) 워커 자체가 안 켜져 실질 영향 없음 — 같은 기능군.
  - key: SPRINTABLE_MEMO_DISPATCHER_POLL_INTERVAL_MS
    reason: SPRINTABLE_RUNTIME_ROLE이 web으로 떨어지면(기본) 워커 자체가 안 켜져 실질 영향 없음 — 같은 기능군.
  - key: SPRINTABLE_TEAMS_OUTBOUND_POLL_INTERVAL_MS
    reason: SPRINTABLE_RUNTIME_ROLE이 web으로 떨어지면(기본) 워커 자체가 안 켜져 실질 영향 없음 — 같은 기능군.

# ⑤㉠(story #2296 후속, 파울로군 지시 2026-07-28) — «알려진 채무» 래칫 baseline.
# ⛔code_read_exempt(위)와 다르다 — exempt는 "코드로 확認해 영구히 정상"이고, 이건 "아직
# triage 안 됐지만 지금 당장 FAIL로 막으면 가드가 꺼지니 일단 알고 있다"다. baseline에
# 없는 새 ㉠(high)가 생기면 즉시 FAIL — 그래서 baseline 목록이 늘어나는 건 이 가드가
# 막으려는 바로 그 방향이다(새 구멍 추가는 이 파일을 고치는 게 아니라 실제로 값을
# 채우는 걸로 막혀야 한다).
# reason/declared_by/until 필수(최대 30일) — mcp-path-contract-allowlist.yml(#2280)과
# 동일 계약. 2026-08-07 실측 기준 15건(#2510 NEXT_PUBLIC_TOSS_CLIENT_KEY 추가로 14→15) —
# check_env_drift.py 실행 로그의 "⑤㉠ baseline" 카운트가 이 숫자와 일치해야 정상(달라지면
# 파일이 낡은 것).
code_read_high_baseline:
  - key: AGENT_INBOX_HMAC_SECRET
    reason: >-
      apps/web/src/services/inbox-item.service.ts — 기본값 없음, 미triage.
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: APP_BASE_URL
    reason: >-
      apps/web/src/lib/auth/cookies.ts — 기본값 없음, 미triage(Amplify 레거시 override 후보).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: FIREBASE_AUTH_ISSUE_SESSION
    reason: >-
      apps/web/src/app/api/auth/firebase/session/route.ts — 기본값 없음, 미triage.
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: FIREBASE_AUTH_MOBILE_ISSUE
    reason: >-
      apps/web/src/app/auth/native/route.ts — 기본값 없음, 미triage.
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: FIREBASE_OAUTH_HANDOFF_ENABLED
    reason: >-
      apps/web/src/app/auth/oauth-handoff/route.ts — 기본값 없음, 미triage(기능 플래그 추정).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: MCP_ALLOWED_TOKEN_REFS
    reason: >-
      apps/web/src/lib/mcp-secrets.ts — 기본값 없음, 미triage.
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: NEXT_PUBLIC_APP_URL
    reason: >-
      apps/web/src/lib/auth/cookies.ts 등 — 기본값 없음, 미triage.
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: NEXT_PUBLIC_FIREBASE_API_KEY
    reason: >-
      apps/web/src/lib/auth/firebase-client.ts — 기본값 없음, 미triage. NEXT_PUBLIC_*
      빌드타임 인라이닝 모호성 있음(AC6 한계 참고).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: NEXT_PUBLIC_FIREBASE_APP_ID
    reason: >-
      apps/web/src/lib/auth/firebase-client.ts — 기본값 없음, 미triage(위와 동일 축).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    reason: >-
      apps/web/src/lib/auth/firebase-client.ts — 기본값 없음, 미triage(위와 동일 축).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: NEXT_PUBLIC_FIREBASE_AUTH_ENABLED
    reason: >-
      apps/web/src/lib/auth/firebase-client.ts — 기본값 없음, 미triage(기능 플래그 추정).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
  - key: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    reason: >-
      apps/web/src/lib/auth/firebase-client.ts·lib/db/server.ts — 기본값 없음, 미triage(위와 동일 축).
      2026-08-28 일괄 만료로 파이프라인 전면 차단 해소 — 실 triage는 story #3168로 이관.
    declared_by: PO
    until: "2026-09-26"
