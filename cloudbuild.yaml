# D-S2/D-S7: Cloud Build — Frontend + Backend 멀티 이미지 빌드
# GCP 프로젝트: sprintable-494803 | 리전: asia-northeast3
#
# 트리거: main→prod, develop→dev (GitHub Actions cloud-build.yml)
# 이미지 태그: $COMMIT_SHA + $ENV (latest-dev / latest-prod)
# 캐시: Artifact Registry 레이어 캐시

substitutions:
  _AR_REGION: asia-northeast3
  _AR_REPO: sprintable
  # story #3079 — 기본값 "false"(=배포 진행). GHA gate 스텝(cloud-build.yml)이 계산해 넘기지
  # 않는 호출(수동 gcloud builds submit 등)에서도 안전측(배포)으로 fail — 스킵은 정확히
  # "true" 문자열일 때만.
  _REALTIME_GCE_SKIP: "false"
  _REALTIME_CLOUDRUN_SKIP: "false"
  _DEPLOY_ENV: dev
  _FASTAPI_URL: https://sprintable-backend-dev-787818285179.asia-northeast3.run.app
  # story #2701 — dev-safe 기본값(빈 문자열 = 이 env var 자체를 안 싣는다, 아래 deploy-mcp
  # 참조). GHA cloud-build.yml이 prod에서만 실값으로 override.
  _MCP_ALLOWED_HOSTS: ''
  # story #2777(어드민 개입 액션) — admin_auth.require_admin_operator()의 ID-token 검증
  # 축(발급자=admin-web-dev 서비스 SA·수신측 audience=이 backend 서비스 자신). 페드루군
  # gcloud describe 실측(2026-08-18) — ⚠️audience는 format-exact(services list의 project
  # 번호 표기와 describe의 canonical hash 표기가 다르면 401, 위 _FASTAPI_URL 재사용 금지 —
  # 이 값은 반드시 describe 출력 그대로). prod는 아래 배포 스텝의 _DEPLOY_ENV 가드로 절대
  # ENV_VARS에 안 실린다(REDIS_URL과 동일 이중가드 — 여기 값이 남아 있어도 안전) + 라우터
  # 자체의 _reject_prod() 가드까지 삼중.
  _ADMIN_OPERATOR_AUDIENCE: https://sprintable-backend-dev-57iommnikq-du.a.run.app
  _ADMIN_OPERATOR_ALLOWLIST: sprintable-admin-web@sprintable-494803.iam.gserviceaccount.com
  # story #2887(BE 아바타 업로드) — dev 버킷(sprintable-avatars-dev, public-read·uniform
  # bucket-level access·cloudrun-runtime-dev=objectAdmin — 페드루 확定 2026-08-21).
  # ⛔story #3117(2026-08-26, prod 실사고 — 선생님 아바타 업로드 503) 후속: 당시 "prod 버킷
  # 미프로비저닝이라 prod에는 이 키 자체가 없어야 안전"이라 dev만 실었던 게 그대로 실사용에서
  # 터졌다(AVATAR_UPLOAD_NOT_CONFIGURED fail-closed). prod 버킷(gs://sprintable-avatars-prod,
  # 동형 IAM+CORS)을 PO가 프로비저닝 완료해 그 전제가 사라졌다 — 아래 배포 스텝에서 REDIS_URL/
  # ADMIN_OPERATOR_*와 달리 이제 dev/prod 둘 다 명시 값으로 싣는다(prod는 substitution이 아닌
  # 버킷명 리터럴 — DATABASE_URL_PROD류처럼 prod 값은 여기 substitutions에 안 둔다).
  _GCS_AVATARS_BUCKET: sprintable-avatars-dev
  # story #2771(pptx 서버 변환, doc 84ef0cb7 §7-4) — office-converter는 아래 deploy-office-
  # converter 스텝이 매 배포마다 재생성/갱신한다. **부트스트랩 완료(페드루군, 2026-08-19)**:
  # 최초 배포가 --port 누락(Gotenberg는 3000, Cloud Run 헬스체크 기본 8080)으로 Ready 실패
  # 했었다 — 수동 --port=3000 적용으로 리비전 00002 Ready 확認(위 deploy 스텝에 반영됨) 후
  # `gcloud run services describe office-converter-dev --region=asia-northeast3
  # --format='value(status.url)'`로 아래 canonical 해시 URL 실측(_ADMIN_OPERATOR_AUDIENCE와
  # 동일 표기 원칙 — services list의 project번호 표기 아닌 describe의 canonical hash 그대로).
  # invoker 바인딩(cloudrun-runtime-dev=backend-dev 런타임 SA 실명 일치, 무인증 403 확認)도
  # 수동 적용 완료 — 위 deploy 스텝의 순서 재구성으로 다음 배포부턴 자동 수렴.
  _GOTENBERG_SERVICE_URL: 'https://office-converter-dev-57iommnikq-du.a.run.app'
  _OFFICE_CONVERTER_MAX_INSTANCES: '3'
  _BACKEND_MIN_INSTANCES: '1'
  # ⚠️ default '1'(아래) = _DEPLOY_ENV:dev default 와 정합한 **dev-safe fallback**(수동 `gcloud builds submit`
  #    override 없을 때 무리한 maxScale 재현 차단). GHA cloud-build.yml 이 per-env override(prod=5·dev=8, #2074 응급).
  #    rollout-safe 산식(ee7794eb·dev TooManyConnections 인시던트): **2 × maxScale × (pool+overflow + raw) +
  #    headroom ≤ max_conn**. per-instance = pool(3/1=4) + pg_pubsub raw(1) = 5.
  #    ⚠️ story #2040(2026-07-20) PO gcloud 실측 갱신 — max_connections이 dev/prod 둘 다 **200**이다
  #    (dev는 100→200 그날 사고대응으로 상향). 이 주석이 한동안 100 가정으로 낡아 있었다(codex #2074
  #    조사가 재적발) — SSOT는 `docs/db-connection-budget.md`, 값 바뀌면 그 문서와 여기를 같이 갱신할 것.
  #    prod: maxScale 5 → 2×5×5+20=70 ≤ 200(여유 65%). dev: maxScale 8 → 2×8×5=80 ≤ 200(여유 60%).
  #    (PgBouncer 풀러: Cloud Run raw TCP 미지원으로 중앙 불가·관리형 풀링 Enterprise Plus 전용 → #1779
  #    폐기 이력 있음. 트래픽↑ 時 아키텍처 재검토(#2074) 필요.)
  _BACKEND_MAX_INSTANCES: '1'
  _DB_POOL_SIZE: '3'
  _DB_MAX_OVERFLOW: '1'
  # ⚠️ story #2442(2026-08-03 P0) — dev-safe fallback(위와 동일 컨벤션, #2040에서 이미 검증된
  #    dev 예산 그대로). prod override는 GHA cloud-build.yml에서 온다(_BACKEND_MAX_INSTANCES와
  #    동일 배선 패턴). prod가 겪은 실제 P0(08:44Z, QueuePool 고갈·/api/v2/health 전면 타임아웃):
  #    Cloud SQL sprintable-prod(max_connections 200)는 RUNNABLE·건강했으나, PgBouncer가 prod에
  #    없거나 무력해(db_pgbouncer=False가 코드+배포 양쪽 기본값 — 위 주석의 "PgBouncer 풀러 불가"
  #    참고) 인스턴스당 3+1=4로 tiny한 앱 풀이 Cloud SQL에 직결돼 실 동시성을 못 버텼다. PO가
  #    gcloud로 20/10(rev 00257-wdz)까지 올려 즉시 회복 확認(health 8/8 200) — 그 손 값을 SSOT로
  #    durable화한다("손 값은 다음 배포가 덮는다" 교훈 재적용).
  #    안전식(AC4, 롤아웃 배수 없이): maxScale(5)×(pool+overflow)+raw(1) = 5×30+1 = 151 ≤ 200
  #    (25% 여유). ⚠️잔여 리스크(문서화, 미해소) — `docs/db-connection-budget.md`의 롤아웃-인식
  #    산식(2×N×per_instance, N=동시생존 리비전수)으로 보면 2×5×31=310>200. main 배포 빈도가
  #    develop보다 훨씬 낮아 롤아웃 중첩 창이 짧다는 판단으로 이 잔여 리스크를 감수한다(PO 확認
  #    필요 — 재검토 트리거: num_backends가 150에 근접하거나 main 연속배포가 잦아질 때).
  # story #2445 Phase1(2026-08-03, 페드루 인프라) — dev VM PgBouncer(10.178.0.63:6432,
  # transaction-mode) 라이브 검증 완료. dev-safe 기본값 'false'(현 동작 유지 — PgBouncer
  # 미경유 시 database.py의 db_pgbouncer 코드 기본값과 동일). prod override는 없음(prod는
  # #2442에서 이미 앱 풀 20/10으로 대응 — PgBouncer 도입은 이 스토리 스코프 밖, 별건).
  # true 전환은 GHA cloud-build.yml dev 분기에서만(DATABASE_URL/DATABASE_URL_DIRECT 시크릿
  # 재바인딩과 반드시 같은 배포에 동반 — 시크릿만 바뀌고 이 값이 false면 statement_cache
  # 미비활성으로 PgBouncer 아래서 prepared statement reuse 깨짐, #1314류 재발).
  _BACKEND_DB_PGBOUNCER: 'false'
  _GA4_MEASUREMENT_ID: ""  # dev 기본값 빈 문자열 — prod 트리거에서 G-RYHFE7XM3X 주입
  # story #2728 후속(2026-08-17, 페드루 PO 지시) — 유나 실측: dev FE 빌드가 NEXT_PUBLIC_
  # EE_ENABLED 자체를 build-arg/Dockerfile ARG 어디에도 배선한 적이 없어(grep 확認 — 이
  # substitution·GHA·Dockerfile 전부 무배선) billing 표면이 dev서 아예 렌더 안 됐다. 배포
  # SSOT/과거 스토리 grep으로 "의도된 결정"이 아님을 확認(manual-env-allowlist.yml에
  # declared_by:PO·미triage로만 남아있던 값 — 코드로 differentiate된 적이 없다). dev-safe
  # 기본값 'false'(SSE_MULTIPLEX_ENABLED와 동일 컨벤션) — GHA가 dev 브랜치에서만 'true' 주입,
  # prod는 이 커밋으로 무접촉(기존 동작 그대로 유지, 명시 false로 durable화만).
  _EE_ENABLED: "false"
  # story #3260(2026-08-31, 페드루 PO 조건부 승인) — 「UI는 있는데 서버가 없음」 결함 클래스
  # 재발 방지: Support Gateway(#f2a27d2a) 착지·AC2/AC3 실측 前까지 위젯 런처를 prod에
  # 노출하지 않는다. EE_ENABLED와 동일 컨벤션 — dev-safe 기본값 false, GHA가 dev만 true 주입.
  _SUPPORT_WIDGET_ENABLED: "false"
  # story #3260 Phase 2 — 위젯이 Support Gateway를 직접 호출(BFF 프록시 없음)하려면 FE
  # 빌드가 그 서비스 origin을 알아야 한다. 빈 문자열 기본값(dev-safe — GHA가 dev 분기에서만
  # 실 Gateway URL 주입, prod는 위젯 자체가 아직 안 뜨므로(SUPPORT_WIDGET_ENABLED=false)
  # 무접촉).
  _NEXT_PUBLIC_SUPPORT_GATEWAY_URL: ""
  # story #3263(지원v1·5에스컬레이션, 페드루 PO 확定 2026-08-31) — 트랜잭셔널 메일의
  # "즉시 고객센터로 문의" 문구가 위젯 승격보다 먼저 prod에 뜨면 "가리키는 표면이 고객
  # 화면엔 없는" fiction이 새 형태로 재발한다(위젯 자체가 아직 prod 미노출, 위 SUPPORT_
  # WIDGET_ENABLED 참고). 이 backend 플래그를 위젯의 그 플래그와 **같은 SSOT·같은 승격
  # 커밋**으로 묶어 순서를 구조로 보장한다 — dev-safe 기본값 false, GHA가 dev만 true 주입,
  # prod는 위젯 자체가 prod에 뜨는 그 커밋에서 함께 true로 바뀐다(그 전까지는 이 값도
  # SUPPORT_WIDGET_ENABLED도 둘 다 false로 정직하게 머문다).
  _SUPPORT_CONTACT_SURFACE_WIDGET: "false"
  # story #3263(같은 스토리 AC1/2, 2026-09-01 AC4 커밋에서 GHA 배선으로 정정) — 에스컬레이션
  # 게이트/DM의 requester(moonklabs org 안 "Sprintable 지원" 에이전트 멤버)·designated
  # approver(PO) team_members.id. PO가 운영 액션(POST /api/v2/team-members)으로 이미 만든
  # 값이라 코드가 새로 정하는 건 아니지만, 최초 계획(Cloud Build 트리거 substitution
  # override로 PO가 수동 유지)은 다른 dev-safe 플래그들과 일관성이 떨어지고 사람이 놓치기
  # 쉬워 GHA dev 분기 주입으로 통일한다(SUPPORT_WIDGET_ENABLED류와 동일 패턴, 페드루 PO
  # 확定 2026-09-01) — 여기 substitution 기본값은 빈 문자열(dev-safe) 그대로, 실 값은
  # .github/workflows/cloud-build.yml dev 분기가 채운다. prod는 무접촉(빈 문자열 유지 —
  # 이 스토리 스코프가 dev 검증까지). 미설정이면 backend가 정직하게 배달
  # skip(support_gateway_token.py::receive_escalation_event 503).
  _SUPPORT_ESCALATION_REQUESTER_MEMBER_ID: ""
  _SUPPORT_ESCALATION_APPROVER_MEMBER_ID: ""
  # story #3263 — 게이트/DM이 실릴 moonklabs org/project. slug 기반(환경별 UUID 무관 안정
  # 참조) — 두 환경 다 동일 실사용 org/project라 dev-safe 기본값 그대로 prod에도 유효.
  _SUPPORT_ESCALATION_TARGET_ORG_SLUG: "moonklabs"
  _SUPPORT_ESCALATION_TARGET_PROJECT_SLUG: "sprintable"
  # story #2758(2026-08-18, 페드루 PO 지시 — EE_ENABLED·LICENSE_CONSENT와 동형 「등재≠배선」
  # 3번째, 미르코 2754 라이브 실증이 이 마디에서 잡음) — apps/web/src/ee/components/billing/
  # toss-checkout.ts(story #2510)가 읽는 Toss 결제위젯 clientKey가 manual-env-allowlist.yml에
  # declared_by:PO로 등재만 되고 build-arg 배선이 0건이었다 — dev FE 런타임에 값이 아예 없어
  # SDK 초기화 직전에서 막혔다. publishable 값(secret 아님, 브라우저 노출 전제) — dev-safe
  # 기본값 빈 문자열 — GHA가 dev 브랜치에서만 Secret Manager TOSS_PAYMENTS_CLIENT_KEY_DEV를
  # 읽어 주입. prod는 이 커밋으로 무접촉(값 반입은 심사 후 선생님 허가 축).
  _TOSS_CLIENT_KEY: ""
  # story #e6500272 후속(2026-08-18, 페드루 PO 지시 — #3188과 동형 발견) — backend
  # `settings.is_ee_enabled`가 읽는 `LICENSE_CONSENT`도 EE_ENABLED와 동일하게 배포 SSOT
  # 어디에도 배선된 적이 없다(cloudbuild.yaml·GHA·Dockerfile 전부 무배선). dev·prod 둘 다
  # 현재 수동 env로만 'agreed' — 이 커밋은 **그 현행값을 그대로 durable화**할 뿐 동작 변경
  # 0(EE_ENABLED와 달리 dev/prod 값이 같다 — "다음 배포가 손 값을 덮는다" 교훈 재적용,
  # `_BACKEND_MIN_INSTANCES`류와 동일 클래스).
  _LICENSE_CONSENT: "agreed"
  # story aec3ec09([P1 후속] OAuth 핸드오프 활성화) — backend `firebase_oauth_handoff_enabled`
  # (config.py 기본 False)도 LICENSE_CONSENT/EE_ENABLED와 동일 클래스로 배포 SSOT에 배선한다
  # (손 gcloud 금지 — 다음 배포가 덮는 함정 회피). dev-safe 기본값 "false"(현행 무변화) —
  # GHA가 dev 브랜치에서만 "true"로 override. prod는 dev 실기기 왕복 통과 전까지 false 유지.
  _FIREBASE_OAUTH_HANDOFF_ENABLED: "false"
  # story #2749(2026-08-18, 페드루 PO 실측 — 가입 인증메일 verify 링크가 dev에서 prod
  # 도메인(app.sprintable.ai)으로 발급됨) — auth.py(verify-email×2·reset-password)가 읽는
  # `NEXT_PUBLIC_APP_URL`이 배포 SSOT 어디에도 배선된 적이 없다(cloudbuild.yaml·GHA 전부
  # 무배선 — grep 확認). 코드 기본값(하드코딩 prod URL)이 dev에도 그대로 새어 dev 발급
  # 메일 링크가 prod로 갔다. dev-safe 기본값은 prod URL(수동 gcloud submit 시 최소한
  # prod와 같은 안전값) — GHA가 dev 브랜치에서만 dev 도메인으로 override.
  _NEXT_PUBLIC_APP_URL: "https://app.sprintable.ai"
  # [P1] iOS TestFlight 구글 로그인 후 404 인시던트(story 57526f21, #3499 PR 리뷰 delta) —
  # /.well-known/apple-app-site-association 라우트(apps/web)가 읽는 env 2종. dev-safe
  # 기본값(수동 gcloud builds submit override 없을 때) — GHA cloud-build.yml이 main에서
  # MOBILE_APP_LINK_ORIGIN만 prod로 override(_FASTAPI_URL과 동일 배선 패턴). Team ID는
  # AASA 응답으로 공개되는 값이라 시크릿 아니고 dev/prod 동일해 override 불요.
  _APPLE_TEAM_ID: "JN798BC4KC"
  _MOBILE_APP_LINK_ORIGIN: "https://dev-app.sprintable.ai"
  # story #3118(Sign in with Apple, PO 확定 2026-08-26) — backend가 읽는 Apple OAuth
  # provider 식별자 2종. Services ID(=OAuth client_id)·Key ID 둘 다 공개돼도 무해한
  # 식별자(시크릿 아님, 위 _APPLE_TEAM_ID와 동일 원칙) — dev/prod 동일 값(PO가 app/
  # dev-app 콜백 도메인 양쪽을 이 하나의 Services ID에 이미 등록 済, override 불요).
  # 실제 비밀(SIWA 개인키)은 여기 두지 않는다 — deploy-backend 스텝의 --update-secrets
  # (Secret Manager `APPLE_SIWA_PRIVATE_KEY`)로만 바인딩.
  _APPLE_SERVICES_ID: "ai.sprintable.web"
  _APPLE_KEY_ID: "DF2G3UV649"
  # story #2078(E-ARCH 0단계) — SSE 멀티플렉서 롤백 플래그. dev-safe 기본값 'false'(수동
  # `gcloud builds submit` override 없을 때 안전). GHA가 dev 브랜치에서만 'true' 주입 —
  # prod는 develop→main 일괄승격에 검증 안 된 플래그를 얹지 않는다(오르테가군 판정,
  # 2026-07-21 — 문제 생기면 48커밋 중 원인 격리 불가라 별도 판단으로 미룸).
  _SSE_MULTIPLEX_ENABLED: "false"
  # story #2005(Phase B P1-c, E-A2A-PROTO 리라이어빌리티): Cloud Run 요청 타임아웃이
  # `deploy-backend` 스텝에 `--timeout` 플래그로 명시된 적이 한 번도 없어(grep 확認) 인프라
  # 기본값에 암묵 의존 중이었다(A2A `/rpc` 특히 `SendStreamingMessage` SSE가 사실상 무기한
  # 실행되는 배경). PO가 라이브 `gcloud`로 실측한 현재 유효값(2026-07-17, 이 세션은 gcloud
  # 접근 없이 그 실측을 신뢰): backend-prod=300s · backend-dev=3600s · mcp-prod=300s
  # (concurrency=80). 아래 기본값(3600)은 `_DEPLOY_ENV: dev` 기본값과 정합한 dev-safe
  # fallback(위 `_BACKEND_MIN_INSTANCES` 주석과 동일 컨벤션) — dev 측정값을 그대로 명시해
  # "인프라 기본값 암묵 의존"에서 "이 파일에 명시된 의도값"으로 전환한다. prod override(300)는
  # `.github/workflows/cloud-build.yml`의 `steps.env`(브랜치별 backend_timeout 산출 후
  # `--substitutions`로 주입, `_BACKEND_MAX_INSTANCES`와 동일 배선)에서 온다 — 이 저장소의
  # "트리거"는 별도 GCP Cloud Build Trigger 리소스가 아니라 그 GHA 워크플로우 자체(파일 상단
  # 주석 "트리거: main→prod, develop→dev (GitHub Actions cloud-build.yml)" 참조)라 prod override
  # 도 이 레포 안에서 완결된다(PO/infra-lane이 GCP 콘솔에서 별도로 설정할 트리거-레벨
  # override는 존재하지 않음 — 확認 후 아래 GHA 워크플로우도 함께 배선함).
  _BACKEND_TIMEOUT: '3600'

  # story #3259(지원v1·1경계) — Support Gateway. deploy-realtime과 동일 하드게이트 컨벤션:
  # 기본 skip(false), PO가 전용 서비스계정·전용 Cloud SQL 인스턴스·Secret Manager 값(아래
  # _SUPPORT_GATEWAY_SERVICE_ACCOUNT/DATABASE_URL/TOKEN_SECRET 시크릿명)을 실제로 프로비저닝한
  # 뒤 이 값을 'true'로 뒤집는다(README.md "프로비저닝(PO 레인)" 참고). ⛔fleet 서비스 계정
  # (backend-dev 런타임 SA)을 여기 재사용하지 않는다 — 그러면 "fleet 자격 0"이 배포 시점에
  # 깨진다. 시크릿도 backend의 DATABASE_URL_*/JWT_SECRET과 별개 이름만 쓴다.
  _SUPPORT_GATEWAY_DEPLOY_ENABLED: 'false'
  _SUPPORT_GATEWAY_SERVICE_ACCOUNT: ''
  _SUPPORT_GATEWAY_MIN_INSTANCES: '0'
  _SUPPORT_GATEWAY_MAX_INSTANCES: '2'
  # story #3260(위젯 셸) — 브라우저가 이 서비스를 직접 호출(BFF 프록시 없음)하므로 실
  # CORS preflight가 돈다. `|`-구분 기본값(빈 문자열=미설정) — deploy-support-gateway
  # 스텝에서 `,`로 변환(cloudbuild.yaml MCP_ALLOWED_HOSTS 선례와 동일 2-layer 콤마 함정 회피).
  _SUPPORT_GATEWAY_CORS_ORIGINS: ''

  # E-ARCH 1단계(story #2078·PR #2358 PG_LISTEN_ENABLED 배선): SSE/LISTEN 전용
  # `sprintable-realtime-${_DEPLOY_ENV}` 서비스 — REST(`sprintable-backend-*`)와 같은 이미지,
  # PG_LISTEN_ENABLED만 다르다(realtime=true·api는 FE 전환 후 PO가 별도로 false 배선 예정,
  # 이 파일은 아직 손대지 않는다 — 순서 뒤바뀌면 FE 전환 전에 api LISTEN이 꺼져 실시간이 죽는다).
  # 아래 기본값은 PO가 2026-07-21 dev에 손으로 생성·검증한 실측값을 그대로 옮긴 것
  # (`gcloud run services describe sprintable-realtime-dev` 대조 완료) — "손 값은 다음
  # 배포가 덮는다" 규율대로 이 파일에 명시해 durable화한다. 장수 SSE 연결이 콜드스타트에
  # 끊기지 않도록 minScale 2(backend는 3) · timeout 3600(요청 타임아웃 상한).
  _REALTIME_MIN_INSTANCES: '2'
  _REALTIME_MAX_INSTANCES: '8'
  _REALTIME_TIMEOUT: '3600'
  # ⛔기본값을 dev URL 에서 빈 문자열로 내린다(오르테가군 PO, 2026-07-23 prod 승격 preflight
  # 에서 적발). 이 값은 `deploy-frontend` 의 `--update-env-vars=REALTIME_URL=${_REALTIME_URL}`
  # 로 흘러 FE 의 `/api/event-stream` 프록시 목적지가 된다. 그런데 GHA 워크플로우의 prod 분기는
  # `realtime_url` output 을 아예 내보내지 않아 `_REALTIME_URL=` (빈 값)이 전달된다 —
  # **"빈 값으로 전달"이 "기본값으로 폴백"이 되는 순간 prod 프론트가 dev realtime 을 가리킨다.**
  # 그 동작에 의존해 안전을 주장할 근거가 이 저장소에 없었다(빈 값 override 를 실측한 전례 0건:
  # `_GA4_MEASUREMENT_ID` 는 기본값도 빈 문자열이라 대조가 안 되고, 나머지는 전부 비어있지 않은
  # 값을 넘긴다). ⇒ 폴백 자체를 안전한 쪽으로 뒤집는다 — 빈 값이면 FE 가 FASTAPI_URL 로 폴백하는
  # 것이 이미 설계된 동작이고(deploy-frontend 주석 "이 값을 비우면 즉시 원복"), 그것이 현행 prod
  # 동작과 정확히 같다. dev 는 워크플로우가 항상 명시값을 넘기므로 무영향(라이브 대조 확認:
  # sprintable-frontend-dev REALTIME_URL=…realtime-dev…run.app).
  # ⚠️이 한 줄은 develop 에도 같이 들어가야 한다 — 승격 브랜치에만 남으면 main/develop 이
  # 또 갈라진다(오늘 0163 에서 겪은 그 부채와 같은 종류). 별 PR 로 동반 반영할 것.
  _REALTIME_URL: ""

  # story c4c72eb1 후속(2026-07-22, GCE 조사 중 발견): `deploy-frontend` 스텝에 `--timeout`
  # 플래그가 한 번도 없었다(story #2005가 backend에서 고친 것과 동형 결함, frontend는
  # 그 처리를 안 받았음) — 인프라 기본값이 아니라 `deploy_frontend.sh`의 옛 `--timeout=60`
  # (일반 페이지 요청 기준값, `/api/event-stream` SSE 프록시 라우트 생긴 뒤 재검토 안 됨)이
  # `gcloud run deploy`의 "플래그 생략 시 기존 값 preserve" 동작으로 암묵 지속되고 있었다.
  # 라이브 실측(2026-07-22, dev/prod 둘 다 60) 그대로 여기 명시해 durable화 — **값을 올리는
  # 것이 아니라 지금 값을 IaC에 고정하는 것**(오르테가군 판정: frontend concurrency=80·
  # maxScale=3(상한 240·짧은 페이지 요청용 sizing) vs realtime concurrency=80·maxScale=8
  # (상한 640·SSE용 sizing) — SSE를 frontend에 오래 붙들면 그 작은 상한을 페이지 요청과
  # 다투게 만드는 새 병목이라, 값 인상은 브라우저→realtime 직결 설계(B) 이후로 미룸).
  # ⚠️ 현행값 고정(2026-07-22) — 이 값이 옳다는 뜻이 아니다. 브라우저 SSE가 `/api/
  # event-stream`(=이 컨테이너)을 타고 있어서 이 60초가 67.5초 주기 절단의 원인이다
  # (story #2101). SSE가 realtime에 직결하도록 정리되면 이 값은 원래 목적(짧은 페이지
  # 요청) 기준으로 재검토할 것 — "왜 60이지? 짧은데?" 하고 무심코 올리면 240 슬롯을
  # 페이지 요청과 SSE가 다투게 만드는 방향이라 근거 없이 만지지 말 것.
  _FRONTEND_TIMEOUT: '60'

  # story #2077(E-ARCH 0단계, 2026-07-24): `deploy-frontend` 스텝에 min/max-instances 플래그
  # 자체가 한 번도 없었다(backend·realtime은 둘 다 배선돼 있었는데 frontend만 빠짐 — story
  # #2005/backend와 동형 결함이 또 frontend에서 재발). "gcloud run deploy는 미지정 설정을
  # preserve"라 다음 배포가 즉시 덮진 않지만, 리뷰·재현이 불가능하고(minScale=3이 왜 3인지
  # 코드에 없음) 서비스 재생성/전체교체 경로를 타면 조용히 기본값(minScale 0)으로 복귀한다.
  # ⭐이 minScale 상향은 #2074(선생님 실기기 "채팅 첫 진입 5~8초") 응급조치다 — 근본은
  # "no available instance" abort(인스턴스 부족→콜드스타트 대기→요청 폐기). backend는 #2415로
  # SSOT에 박혀 닫혔으나 frontend는 아직 코드 밖에 떠 있었다. 아래 dev-safe 기본값은 라이브
  # 실측 그대로(2026-07-24, `gcloud run services describe sprintable-frontend-dev` 대조:
  # latestReady==latestCreated·traffic 100%인 서빙 리비전 자체를 확認, template spec만 본 것
  # 아님 — 오늘 prod FE 22시간 무실효 사고의 3축 교훈 적용). GHA가 prod에서 override(3/10).
  _FRONTEND_MIN_INSTANCES: '1'
  _FRONTEND_MAX_INSTANCES: '3'
  # concurrency=80은 Cloud Run 플랫폼 기본값과 라이브 실측(dev describe 확認)이 이미 일치 —
  # SSOT에 값을 안 박아도 드리프트 위험이 없다(플랫폼 기본값 자체가 곧 이 값). 굳이 플래그로
  # 고정하지 않는다(불필요한 명시로 "왜 80이지" 오해를 만들 필요 없음) — 판단 근거만 여기 기록.
  # startup-cpu-boost=true — `gcloud run deploy --help`: "Enabled by default when unspecified
  # on new services"(신규 서비스 생성 시엔 기본 on이라 minScale=0류처럼 조용히 꺼지는 위험은
  # 아니다). 그래도 "기존 서비스 update 시 미지정=현재값 preserve"와 "신규 생성 시 기본값"이
  # 서로 다른 경로라 애매함이 남는다 — 아래 deploy-frontend에 `--cpu-boost`로 명시해 두 경로
  # 모두 동일 결과가 되게 고정한다(dev/prod 공통값이라 별도 substitution 불필요).

  # E-ARCH 1단계 durable화 마지막 조각(story #2078, 2026-07-21 PO 손 플립 확認 후): api
  # (sprintable-backend-*)의 PG_LISTEN_ENABLED. ⚠️ 기본값 'true' — prod는 아직
  # sprintable-realtime-prod가 없다(deploy-realtime 스텝이 prod에서 스킵 중, PO 검증 전).
  # prod api를 false로 먼저 못박으면 prod 승격 시점에 LISTEN 하는 서비스가 하나도 없어
  # 실시간이 완전히 죽는 landmine이 된다 — 그래서 dev만 GHA에서 명시 override(false)하고
  # prod는 이 fallback(true)을 그대로 상속해 오늘 이전과 동일 동작을 유지한다. prod가
  # sprintable-realtime-prod 배선을 마치는 승격 시점에 GHA prod 분기에도 false를 명시할 것.
  _BACKEND_PG_LISTEN_ENABLED: 'true'

  # ⛔E-ARCH S2 정리(story #2078, 2026-07-21) — **전제가 틀렸음이 #2123에서 드러남(아래 정정
  # 참조)**: 당시 "api는 SSE를 서빙하지 않는다"고 판단해 Redis 구독(consume)을 껐다. 그런데
  # `agent_onboarding_config.py::resolve_backend_direct_url()`이 에이전트에게 `/agent/stream`
  # 접속 host로 **FASTAPI_URL(=backend-dev 자신)**을 명시 배포한다(CF-fronted 도메인 금지·
  # backend-direct 강제, SSE 버퍼링 회피 목적) — REALTIME_URL로 옮겨간 적이 없다. 즉 **에이전트
  # SSE 연결은 지금도 backend-dev가 서빙 중**(라이브 실측 2026-07-23: agent/stream 요청
  # backend-dev 95건 vs realtime-dev 2건, 24h 표본). dual_publish(발행)는 그대로 필요.
  # 기본값 'true'로 정정(과거 'false' fallback 폐기) — deploy-backend에서만 쓰이고
  # deploy-realtime은 항상 true를 직접 명시(아래, 변수 아님).
  _BACKEND_REDIS_CONSUME_ENABLED: 'true'

  # story #2123 S0-b 후속(2026-07-23, 오르테가군 PR #2416 리뷰 적발): consume만으로는
  # 부족하다 — `resolve_backplane()`이 `event_broker_redis_dispatch_enabled`도 함께 봐서
  # off면 구독은 해도 실 dispatch(=publish_event/_push_to_agent 호출)를 안 한다(순수 shadow
  # observe). 위와 같은 이유(에이전트 연결이 backend-dev에 있음)로 dispatch도 필요.
  # ⚠️중복배달 검토: dispatch는 각 서비스 **자기 프로세스의 로컬 큐/구독자**에만 push한다
  # (`_agent_connections`/`_subscribers`는 프로세스-로컬 dict, 크로스서비스 공유 없음) — 에이전트는
  # backend-dev에만 연결되고 브라우저는 realtime-dev(프론트 REALTIME_URL 프록시)에만 연결되므로,
  # 두 서비스가 동시에 dispatch=true여도 서로 다른 로컬 큐를 보는 것이라 같은 클라이언트에 2회
  # 배달되는 경로가 없다(전제: 에이전트/브라우저 연결이 겹치지 않음 — 위 라이브 카운트로 확認).
  # 기본값 'false'(dev-safe fallback) — GHA가 dev만 명시 override(true), prod는 Redis 요소
  # 자체가 없어 무의미(false 유지).
  _BACKEND_REDIS_DISPATCH_ENABLED: 'false'

  # story #2135(E-ARCH 근본, 2026-07-23 #2123 실측 적발): 이 값이 실려가는 실제 env var 키가
  # 여태 `REDIS_CONSUME_ENABLED`였다 — Settings 필드는 `event_broker_redis_consume_enabled`
  # (config.py, env_prefix 없음)라 pydantic-settings가 이 키를 매칭 못 하고 `extra="ignore"`가
  # 조용히 삼켜, 위 _BACKEND_REDIS_CONSUME_ENABLED 의도가 api에 **한 번도 적용된 적이 없었다**
  # (필드는 항상 코드 기본값 True로 남았음 — uv run 재현 + 라이브 로그 확認 완료. 결과적으로
  # "의도 false"였던 구간에도 필드는 이미 True였다는 뜻 — 아래 값 정정과 별개로 이 키 이름
  # 버그 자체는 그대로 고친다). 아래 두 곳(deploy-backend/deploy-realtime)의 env var **키
  # 이름**을 `EVENT_BROKER_REDIS_CONSUME_ENABLED`로 정정하고, **구 키(`REDIS_CONSUME_ENABLED`)
  # 는 `--remove-env-vars`로 명시 제거**한다(`--update-env-vars`는 additive라 안 지우면 두 키가
  # 라이브에 공존 — "설정돼 있네"로 오독하게 만드는 바로 그 함정, 오르테가군 PR #2416 리뷰 적발).

  # story #2123 S0-b(2026-07-23, 오르테가군 판정): wake_agent()의 크로스인스턴스 배달은
  # #2122가 병합됐어도 **발행측 스위치(FANOUT_WAKE_REDIS_ENABLED)가 꺼져 있어 무효**였다
  # (agent_gateway.py:224 — 이 플래그 off면 event_broker 완전 우회, 직접 pg_notify만 호출).
  # wake_agent 호출부(stories.py/gates.py/agents.py/agent_dispatch.py/a2a.py)는 전부 REST
  # 라우터라 REST 트래픽이 실제로 향하는 api(backend-dev)에서만 이 스위치가 의미 있다 —
  # realtime-dev/GCE는 REST를 안 받아 wake_agent()를 호출하지 않으므로 배선 불필요(#2123
  # doc §판정 종결 참조). 기본값 'false'(수동 `gcloud builds submit` dev-safe fallback,
  # prod로 잘못 흘러가도 무해) — GHA가 dev만 명시 override(true).
  _BACKEND_FANOUT_WAKE_REDIS_ENABLED: 'false'

  # story #2139/#2143 follow-up (2026-07-23, Ortega's call) -- "turn on the backplane", 3 flags.
  # Different in nature (recorded here per his instruction): PRESENCE_ONLINE has a DB fallback
  # already (not broken today -- turning it on is a freshness improvement, not a bugfix).
  # PRESENCE (chat_presence "working") is a process-local dict -- genuinely differs per GCE
  # node today (real defect). SSE_LEASE is a per-instance counter -- the global concurrent-
  # connection cap doesn't actually hold across nodes today (real defect). Default 'false'
  # (dev-safe fallback) -- GHA overrides dev only (true). Prod needs the realtime GCE 3-node
  # side turned on together (separate staged step, not this commit) -- same staging principle
  # already used for #2120/#2121/#2122.
  _BACKEND_PRESENCE_REDIS_ENABLED: 'false'
  _BACKEND_PRESENCE_ONLINE_REDIS_ENABLED: 'false'
  _BACKEND_SSE_LEASE_REDIS_ENABLED: 'false'

  # story #2158(2026-07-24) 후속(#2077에서 동반 배선) — 재생 버퍼 자체는 머지됐으나(PR #2468)
  # 이 스위치를 켤 배선이 없어 사용자 경로 재측정을 못 하고 있었다. #2158의 취지 자체가
  # "손 gcloud로 켜지 않는다"라 반드시 이 SSOT를 거친다. dev-safe 기본값 'false'(무회귀) —
  # GHA가 dev만 명시 override(true). prod는 손대지 않는다(#2158 flag 자체가 아직 라이브
  # 사용자 경로 검증 전이라 prod에 얹을 단계가 아님 — #2120/#2121/#2122와 동일 단계적 승격 원칙).
  _BACKEND_SSE_TRANSIENT_REPLAY_ENABLED: 'false'

  # ⛔story cd10e123 계열 긴급수정(2026-07-21, codex 리서치 검증 중 적발): REDIS_URL·
  # EVENT_BROKER_REDIS_DUAL_PUBLISH_ENABLED가 여태 여기 어디에도 없는 순수 수동 Cloud Run env
  # 였다 — PG_LISTEN_ENABLED는 durable화됐는데(위) 이 둘은 안 돼서, 서비스가 `--set-env-vars`로
  # 재배포/재생성되면 dispatch 관련 플래그가 조용히 기본값(false/None)으로 되돌아가고 LISTEN은
  # 이미 꺼져있어 **두 전송경로 동시 무효화(실시간 전면정지)** 조건이 성립한다. 기본값은
  # dev-safe fallback(REDIS 미설정 상태와 동등 — config.py의 "fail-safe if not configured"
  # 로직이 그대로 우아하게 no-op) — GHA가 dev/prod 실측값으로 override.
  _REDIS_URL: ''
  _BACKEND_REDIS_DUAL_PUBLISH_ENABLED: 'false'

steps:
  # story #2806 — GCS 첨부 버킷(sprintable-memo-attachments) CORS를 SSOT로 durable화. 실측
  # (디디군, 2026-08-19): 이 버킷은 dev/prod 공유(GCS_MEMO_ATTACHMENTS_BUCKET override가
  # 배포 SSOT 어디에도 없어 코드 기본값 하나를 양쪽이 씀) — 지금까지 CORS가 배포 SSOT에
  # 선언된 적이 0건(레포 전수 grep)이라 브라우저 signed-URL fetch(docx-preview류, PDF는
  # iframe이라 무영향)가 origin 불문 전부 차단돼 있었다. `gcloud storage buckets update
  # --cors-file=`은 **전체 교체**라 매 배포 재실행에도 멱등(파일 내용이 그대로면 결과도
  # 그대로) — build/push와 무관해 초반부터 병행 실행.
  #
  # 갱신(story #3242, 2026-08-31, 페드루 PO 발주·유나 실측 근거): #886d996f 업로드 기능이
  # 이 버킷에 서명 PUT을 직접 쏘게 되며 GET/HEAD만 있던 method가 뚫렸다 — PUT 프리플라이트가
  # Access-Control-Allow-Origin 부재로 net::ERR_FAILED. method에 PUT 추가(OPTIONS는 GCS가
  # Access-Control-Request-Method 매칭으로 자동 처리 — 명시 금지, 공식문서 확認 완료). PUT은
  # 항상 create_only=True로 서명(x-goog-if-generation-match: 0 바인딩)·content_type 바인딩이라
  # responseHeader(=GCS의 Access-Control-Allow-Headers 대응 필드)에 x-goog-if-generation-match를
  # 추가(Content-Type은 기존에 이미 있었음) — 둘 중 하나라도 빠지면 프리플라이트가 여전히 막힌다.
  - id: apply-gcs-attachments-cors
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        gcloud storage buckets update gs://sprintable-memo-attachments \
          --cors-file=infra/gcs-attachments-cors.json \
          --quiet
        applied=$(gcloud storage buckets describe gs://sprintable-memo-attachments --format='value(cors_config)')
        echo "sprintable-memo-attachments CORS applied = $${applied}"
        case "$${applied}" in
          *https://sprintable-frontend-dev-787818285179.asia-northeast3.run.app*) ;;
          *) echo "FAIL: dev Cloud Run regional origin missing from applied CORS — 배포 검증 실패" >&2; exit 1 ;;
        esac
        echo "OK: dev Cloud Run regional origin confirmed in applied CORS"
    waitFor: ['-']

  # story #2887 — avatar 전용 버킷 CORS. CORS json(infra/gcs-avatars-cors.json)
  # 자체는 dev/prod origin을 이미 다 실은 공용 파일(memo-attachments와 동일 관례) — 버킷명만
  # env별로 갈린다.
  # story #3117(2026-08-26, prod 실사고 후속) — 여태 dev 전용 게이트(prod 버킷 미프로비저닝)
  # 였다가, PO가 prod 버킷(gs://sprintable-avatars-prod) CORS를 손으로 1회 적용해 그 유보를
  # 풀었었다 — 그 손적용 자체가 드리프트 씨앗(다음 배포가 검증 안 하면 재적용 없이 방치)이라
  # 여기 SSOT로 편입해 매 배포마다 멱등 재적용+검증하게 한다.
  - id: apply-gcs-avatars-cors
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_DEPLOY_ENV}" == "prod" ]; then
          BUCKET="sprintable-avatars-prod"
        else
          BUCKET="${_GCS_AVATARS_BUCKET}"
        fi
        gcloud storage buckets update gs://$${BUCKET} \
          --cors-file=infra/gcs-avatars-cors.json \
          --quiet
        applied=$(gcloud storage buckets describe gs://$${BUCKET} --format='value(cors_config)')
        echo "$${BUCKET} CORS applied = $${applied}"
        if [ "${_DEPLOY_ENV}" == "prod" ]; then
          case "$${applied}" in
            *https://app.sprintable.ai*) ;;
            *) echo "FAIL: prod origin missing from applied CORS — 배포 검증 실패" >&2; exit 1 ;;
          esac
          echo "OK: prod origin confirmed in applied CORS"
        else
          case "$${applied}" in
            *https://sprintable-frontend-dev-787818285179.asia-northeast3.run.app*) ;;
            *) echo "FAIL: dev Cloud Run regional origin missing from applied CORS — 배포 검증 실패" >&2; exit 1 ;;
          esac
          echo "OK: dev Cloud Run regional origin confirmed in applied CORS"
        fi
    waitFor: ['-']

  # ─── Frontend (Next.js standalone) ────────────────────────────────────────
  - id: build-frontend
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - --no-cache
      - -f
      - apps/web/Dockerfile
      - -t
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/frontend:${COMMIT_SHA}
      - -t
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/frontend:latest-${_DEPLOY_ENV}
      - --build-arg
      - NEXT_PUBLIC_FASTAPI_URL=${_FASTAPI_URL}
      - --build-arg
      - NEXT_PUBLIC_OAUTH_ENABLED=true
      - --build-arg
      - NEXT_PUBLIC_GA4_MEASUREMENT_ID=${_GA4_MEASUREMENT_ID}
      - --build-arg
      - NEXT_PUBLIC_SSE_MULTIPLEX_ENABLED=${_SSE_MULTIPLEX_ENABLED}
      - --build-arg
      - NEXT_PUBLIC_EE_ENABLED=${_EE_ENABLED}
      - --build-arg
      - NEXT_PUBLIC_SUPPORT_WIDGET_ENABLED=${_SUPPORT_WIDGET_ENABLED}
      - --build-arg
      - NEXT_PUBLIC_SUPPORT_GATEWAY_URL=${_NEXT_PUBLIC_SUPPORT_GATEWAY_URL}
      - --build-arg
      - NEXT_PUBLIC_TOSS_CLIENT_KEY=${_TOSS_CLIENT_KEY}
      - .
    env:
      - DOCKER_BUILDKIT=1

  # ─── Backend (FastAPI) ─────────────────────────────────────────────────────
  - id: build-backend
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - backend/Dockerfile
      - -t
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:${COMMIT_SHA}
      - -t
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:latest-${_DEPLOY_ENV}
      - --cache-from
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:latest-${_DEPLOY_ENV}
      - --build-arg
      - BUILDKIT_INLINE_CACHE=1
      - ./backend
    env:
      - DOCKER_BUILDKIT=1

  # ─── Support Gateway (story #3259, 지원v1·1경계) ───────────────────────────
  # 독립 디렉터리·독립 이미지 — backend 레이어를 전혀 상속하지 않는다(build context가
  # ./support-gateway뿐, backend/의 어떤 파일도 이 이미지에 안 들어간다).
  - id: build-support-gateway
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - support-gateway/Dockerfile
      - -t
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/support-gateway:${COMMIT_SHA}
      - -t
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/support-gateway:latest-${_DEPLOY_ENV}
      - --cache-from
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/support-gateway:latest-${_DEPLOY_ENV}
      - --build-arg
      - BUILDKIT_INLINE_CACHE=1
      - ./support-gateway
    env:
      - DOCKER_BUILDKIT=1

  # ─── Push images ──────────────────────────────────────────────────────────
  - id: push-frontend
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/frontend
    waitFor: [build-frontend]

  - id: push-backend
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend
    waitFor: [build-backend]

  - id: push-support-gateway
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/support-gateway
    waitFor: [build-support-gateway]

  # ─── Migrate DB (story dbda0baf, E-RECRUIT S13) ────────────────────────────
  # 근본원인(2026-07-05 dev 인시던트): migrate 잡이 파이프라인과 완전히 분리돼 있어 ⓐ
  # provision_migrate_job.sh 를 COMMIT_SHA 없이 수동 실행하면 마지막 `latest-${ENV}` 태그에
  # 조용히 고정되고(mutable·드리프트) ⓑ 잡 자체가 인라인 `head`(단수)로 수동 override 돼
  # migrate.sh(정본 `heads` 복수)를 안 타는 상태였다. 매 배포마다 잡을 SHA-pinned 이미지 +
  # canonical command(`/app/scripts/migrate.sh`)로 강제 재배포해 드리프트가 다시 쌓일 수 없게 한다.
  - id: update-migrate-job
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        GCP_PROJECT="${PROJECT_ID}" GCP_REGION="${_AR_REGION}" AR_REPO="${_AR_REPO}" COMMIT_SHA="${COMMIT_SHA}" \
          bash backend/scripts/provision_migrate_job.sh ${_DEPLOY_ENV}
    waitFor: [push-backend]

  # `gcloud run jobs execute --wait`는 잡 실패 시 비-0 exit → 이 스텝이 실패 → Cloud Build가
  # 빌드 전체를 실패 처리하고 뒤따르는 deploy-backend(waitFor 의존)는 아예 실행되지 않는다
  # (migrate 실패 시 배포 자동 abort — "잡 성공≠앱DB 배포" 상태가 재발할 수 없음). 실패는
  # Cloud Build 로그 + GHA 워크플로우 실패(빨간 X·커밋 상태)로 가시화된다.
  - id: run-migrate-job
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - jobs
      - execute
      - sprintable-migrate-${_DEPLOY_ENV}
      - --region=${_AR_REGION}
      - --wait
    waitFor: [update-migrate-job]

  # ─── Migrate Support Gateway DB (story #f2a27d2a, flip PR AC4) ─────────────
  # backend의 run-migrate-job과 동형 fail-closed 계약 — 배포 하드게이트(false)일 때도 이
  # 두 스텝은 그냥 스킵(잡 자체가 없거나 provision만 하고 execute는 건너뜀)해 깨지지 않는다.
  - id: update-support-gateway-migrate-job
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_SUPPORT_GATEWAY_DEPLOY_ENABLED}" != "true" ]; then
          echo "skip update-support-gateway-migrate-job: _SUPPORT_GATEWAY_DEPLOY_ENABLED != true"
          exit 0
        fi
        GCP_PROJECT="${PROJECT_ID}" GCP_REGION="${_AR_REGION}" AR_REPO="${_AR_REPO}" COMMIT_SHA="${COMMIT_SHA}" \
          SUPPORT_GATEWAY_SERVICE_ACCOUNT="${_SUPPORT_GATEWAY_SERVICE_ACCOUNT}" \
          bash support-gateway/scripts/provision_migrate_job.sh ${_DEPLOY_ENV}
    waitFor: [push-support-gateway]

  - id: run-support-gateway-migrate-job
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_SUPPORT_GATEWAY_DEPLOY_ENABLED}" != "true" ]; then
          echo "skip run-support-gateway-migrate-job: _SUPPORT_GATEWAY_DEPLOY_ENABLED != true"
          exit 0
        fi
        gcloud run jobs execute sprintable-support-gateway-migrate-${_DEPLOY_ENV} \
          --region=${_AR_REGION} \
          --wait
    waitFor: [update-support-gateway-migrate-job]

  # ─── Deploy to Cloud Run ───────────────────────────────────────────────────
  - id: deploy-frontend
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - sprintable-frontend-${_DEPLOY_ENV}
      - --image=${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/frontend:${COMMIT_SHA}
      - --region=${_AR_REGION}
      - --timeout=${_FRONTEND_TIMEOUT}
      # story #2077 — min/max-instances가 이 스텝에 한 번도 배선된 적이 없었다(backend·realtime은
      # 있었는데 frontend만 빠짐). 현행 라이브 값 그대로 IaC에 고정(값 인상 아님) — 근거는 위
      # substitutions 섹션의 _FRONTEND_MIN/MAX_INSTANCES 주석 참조.
      - --min-instances=${_FRONTEND_MIN_INSTANCES}
      - --max-instances=${_FRONTEND_MAX_INSTANCES}
      # startup-cpu-boost=true는 라이브 실측(dev describe 확認) — 근거는 위 substitutions
      # 섹션 주석 참조(minScale만큼 위험하진 않지만 명시로 애매함을 없앤다).
      - --cpu-boost
      # E-ARCH 1단계(story #2078) — /api/event-stream 프록시 라우트가 REALTIME_URL을 읽어
      # SSE/LISTEN 전용 realtime-gateway로 향한다(REALTIME_URL 없으면 기존 FASTAPI_URL로
      # 폴백 — 이 값을 비우면 코드 변경·재배포 없이 즉시 원복). --update-env-vars(additive).
      # [P1] iOS TestFlight 구글 로그인 후 404 인시던트(story 57526f21) — APPLE_TEAM_ID·
      # MOBILE_APP_LINK_ORIGIN 없으면 /.well-known/apple-app-site-association 라우트가
      # 설계대로 500(fail-loud)을 낸다. 같은 --update-env-vars에 동반(additive, 기존 env 보존).
      # story #3168(2026-08-28, 페드루 PO 실측) — APP_BASE_URL/NEXT_PUBLIC_APP_URL 둘 다
      # 이 스텝에 한 번도 배선된 적이 없어(순수 수동 category C) dev="NEXT_PUBLIC_APP_URL만
      # 유"·prod="APP_BASE_URL만 유"로 환경마다 정반대 절반만 우연히 채워져 있었다 —
      # apps/web/src/lib/auth/cookies.ts의 `APP_BASE_URL ?? NEXT_PUBLIC_APP_URL` 폴백
      # 사슬 덕에 지금까지는 두 환경 다 결과적으로 동작해왔을 뿐(secure 쿠키 판정용),
      # prod의 APP_BASE_URL은 수동 주입값이라 다음 전체 env 교체 배포가 조용히 지울 수
      # 있는 시한 위험이었다. `_NEXT_PUBLIC_APP_URL`(이미 dev="https://dev-app.sprintable.ai"·
      # prod="https://app.sprintable.ai"로 정확히 분기돼 있음, GHA cloud-build.yml 참고)를
      # 재사용해 두 키 다 이 스텝에서 명시 배선 — 폴백 우선순위상 APP_BASE_URL이 먼저
      # 읽히므로 그 값을 정본으로 통일하고 NEXT_PUBLIC_APP_URL은 이미 그 값을 쓰던 dev의
      # 하위호환(레거시 Amplify override 축, app-url.ts 주석 참고)으로 같이 채운다.
      - --update-env-vars=REALTIME_URL=${_REALTIME_URL},APPLE_TEAM_ID=${_APPLE_TEAM_ID},MOBILE_APP_LINK_ORIGIN=${_MOBILE_APP_LINK_ORIGIN},APP_BASE_URL=${_NEXT_PUBLIC_APP_URL},NEXT_PUBLIC_APP_URL=${_NEXT_PUBLIC_APP_URL}
      - --quiet
    waitFor: [push-frontend]

  # story #2771(pptx 서버 변환, doc 84ef0cb7 §7-4) — office-converter: Gotenberg(LibreOffice+
  # Chromium 번들) 격리 배치. 자체 빌드 없음(공개 이미지 그대로) — build/push 스텝 불요,
  # push-backend/push-frontend를 기다릴 필요도 없어 초반부터 병행 실행. ⛔dev 전용 하드게이트
  # (deploy-realtime과 동일 컨벤션) — prod 승격은 별도 PO 판단.
  # 보안(§7-3): --no-allow-unauthenticated — IAM 게이트(ingress 제한 아님, VPC 커넥터 불요).
  # 호출부(backend)가 자신의 런타임 SA로 audience=이 서비스 URL인 ID 토큰을 떠 Authorization에
  # 실어야 통과(office_conversion.py `_id_token_header()`) — admin_auth.require_admin_operator()
  # 의 반대 방향(거긴 backend가 검증자, 여긴 backend가 호출자)과 같은 Cloud Run IAM 패턴.
  # concurrency=1(§3-A: LibreOffice headless 사실상 단일스레드, 다중요청 동거=메모리 누수/좀비
  # 프로세스 실보고) · min=0(열람 트리거라 상시 트래픽 없음, scale-to-zero) · timeout=120s(대형
  # pptx 변환 지연 흡수).
  - id: deploy-office-converter
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_DEPLOY_ENV}" != "dev" ]; then
          echo "skip deploy-office-converter: prod gate pending (story #2771 dev 검증 후 오픈)"
          exit 0
        fi
        # ⛔실배포 핫픽스(페드루군, 2026-08-19) — Gotenberg 이미지는 3000 포트에 듣는데 이
        # --port 플래그가 없어 Cloud Run 기본값(8080)으로 헬스체크해 최초 배포가 Ready 실패
        # 했다(리비전은 뜨지만 트래픽 라우팅 불가). --port=3000 명시로 근본 수정.
        set +e
        gcloud run deploy office-converter-${_DEPLOY_ENV} \
          --image=gotenberg/gotenberg:8.36 \
          --port=3000 \
          --region=${_AR_REGION} \
          --no-allow-unauthenticated \
          --concurrency=1 \
          --min-instances=0 \
          --max-instances=${_OFFICE_CONVERTER_MAX_INSTANCES} \
          --cpu=1 \
          --memory=2Gi \
          --timeout=120 \
          --quiet
        deploy_status=$?
        set -e
        # ⛔구조 수정(페드루군 지적, 2026-08-19) — 이전엔 이 바인딩이 위 deploy 성공(Ready)에
        # 인질 잡혀 있었다: deploy가 Ready 실패(비-zero exit)하면 set -e가 스텝을 그 자리서
        # 죽여 바인딩 명령까지 못 갔다(포트 핫픽스 이전 실측 사고 — invoker 미부여 상태로 방치,
        # 수동 개입 필요). add-iam-policy-binding은 Ready 여부와 무관하게 서비스 리소스만
        # 있으면 동작하므로(deploy가 Ready 실패해도 서비스 자체는 이미 생성돼 있다), deploy
        # 성공 여부와 **분리**해 항상 시도한다(idempotent라 안전) — 그래야 향후 다른 이유로
        # deploy가 일시적으로 non-Ready여도 IAM은 계속 맞는 상태로 수렴하고, deploy 자체의
        # 실패는(가짜로 감추지 않고) 아래에서 그대로 다시 실패시켜 가시성을 유지한다.
        gcloud run services add-iam-policy-binding office-converter-${_DEPLOY_ENV} \
          --region=${_AR_REGION} \
          --member="serviceAccount:cloudrun-runtime-${_DEPLOY_ENV}@${PROJECT_ID}.iam.gserviceaccount.com" \
          --role="roles/run.invoker" \
          --quiet
        if [ "$${deploy_status}" -ne 0 ]; then
          echo "FAIL: office-converter-${_DEPLOY_ENV} deploy did not reach Ready (exit $${deploy_status})."
          echo "      invoker binding was applied regardless — investigate the deploy failure itself."
          exit "$${deploy_status}"
        fi
        served=$(gcloud run services describe office-converter-${_DEPLOY_ENV} \
          --region=${_AR_REGION} --format='value(status.url)')
        echo "OK: office-converter-${_DEPLOY_ENV} = $${served}"
        if [ -z "${_GOTENBERG_SERVICE_URL}" ]; then
          echo "NOTE: _GOTENBERG_SERVICE_URL substitution이 비어 있다 — backend는 이 서비스를"
          echo "      아직 모른다(503 fail-closed). 위 URL을 substitutions에 채워 재배포할 것"
          echo "      (부트스트랩 절차, 위 substitutions 섹션 주석 참고)."
        fi
    waitFor: ['-']

  - id: deploy-backend
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        # ⚠️--update-env-vars(additive) 필수·--set-env-vars(전체wipe) 금지. --allow-unauthenticated
        # 필수(frontend가 NEXT_PUBLIC_FASTAPI_URL로 직접호출·미지정 시 기존IAM preserve로 403 위험,
        # 2026-06-21 prod 사건). story #2005/#2442/#2078/#2123/#2135/#3118 — 타임아웃·DB풀(rollout
        # 산식: 2×maxScale×(pool+overflow+1)≤db max_conn)·PG_LISTEN·EVENT_BROKER_REDIS_*·
        # FANOUT_WAKE·Apple OAuth 식별자(비밀 아님, 개인키는 아래 SECRETS_FLAG로). 전체 배경(사건
        # 경위·근거)은 story #3124(2026-08-26 외부화)로 test_deploy_backend_redis_secret_
        # conflict.py 모듈 docstring 「deploy-backend 인라인 주석 아카이브」 절이 SSOT.
        ENV_VARS="FASTAPI_URL=${_FASTAPI_URL},DB_POOL_SIZE=${_DB_POOL_SIZE},DB_MAX_OVERFLOW=${_DB_MAX_OVERFLOW},DB_PGBOUNCER=${_BACKEND_DB_PGBOUNCER},PG_LISTEN_ENABLED=${_BACKEND_PG_LISTEN_ENABLED},EVENT_BROKER_REDIS_CONSUME_ENABLED=${_BACKEND_REDIS_CONSUME_ENABLED},EVENT_BROKER_REDIS_DISPATCH_ENABLED=${_BACKEND_REDIS_DISPATCH_ENABLED},EVENT_BROKER_REDIS_DUAL_PUBLISH_ENABLED=${_BACKEND_REDIS_DUAL_PUBLISH_ENABLED},FANOUT_WAKE_REDIS_ENABLED=${_BACKEND_FANOUT_WAKE_REDIS_ENABLED},PRESENCE_REDIS_ENABLED=${_BACKEND_PRESENCE_REDIS_ENABLED},PRESENCE_ONLINE_REDIS_ENABLED=${_BACKEND_PRESENCE_ONLINE_REDIS_ENABLED},SSE_LEASE_REDIS_ENABLED=${_BACKEND_SSE_LEASE_REDIS_ENABLED},SSE_TRANSIENT_REPLAY_ENABLED=${_BACKEND_SSE_TRANSIENT_REPLAY_ENABLED},LICENSE_CONSENT=${_LICENSE_CONSENT},NEXT_PUBLIC_APP_URL=${_NEXT_PUBLIC_APP_URL},DEPLOY_ENV=${_DEPLOY_ENV},FIREBASE_OAUTH_HANDOFF_ENABLED=${_FIREBASE_OAUTH_HANDOFF_ENABLED},APPLE_SERVICES_ID=${_APPLE_SERVICES_ID},APPLE_KEY_ID=${_APPLE_KEY_ID},APPLE_TEAM_ID=${_APPLE_TEAM_ID},SUPPORT_CONTACT_SURFACE_WIDGET=${_SUPPORT_CONTACT_SURFACE_WIDGET},SUPPORT_ESCALATION_REQUESTER_MEMBER_ID=${_SUPPORT_ESCALATION_REQUESTER_MEMBER_ID},SUPPORT_ESCALATION_APPROVER_MEMBER_ID=${_SUPPORT_ESCALATION_APPROVER_MEMBER_ID},SUPPORT_ESCALATION_TARGET_ORG_SLUG=${_SUPPORT_ESCALATION_TARGET_ORG_SLUG},SUPPORT_ESCALATION_TARGET_PROJECT_SLUG=${_SUPPORT_ESCALATION_TARGET_PROJECT_SLUG}"
        # story #2141 — REDIS_URL: prod는 Secret Manager 바인딩과 동명 키 충돌 방지로 여기서 안 넘김.
        # ⛔story #2423 — 셸 변수는 반드시 $$로 이스케이프(단일 $는 Cloud Build 자신의 substitution
        # 파서가 args 전체를 먼저 훑어 build submit 자체를 INVALID_ARGUMENT로 거부 — 예시로도 금지).
        if [ "${_DEPLOY_ENV}" != "prod" ]; then
          ENV_VARS="$${ENV_VARS},REDIS_URL=${_REDIS_URL}"
        fi
        # story #2777 — ADMIN_OPERATOR_*: prod는 키 자체가 없어야 안전(require_admin_operator
        # fail-closed 503, 대표승인 前 prod 결제개입 전면금지 태세와 정합).
        if [ "${_DEPLOY_ENV}" != "prod" ]; then
          ENV_VARS="$${ENV_VARS},ADMIN_OPERATOR_AUDIENCE=${_ADMIN_OPERATOR_AUDIENCE},ADMIN_OPERATOR_ALLOWLIST=${_ADMIN_OPERATOR_ALLOWLIST}"
        fi
        # story #3117 — GCS_AVATARS_BUCKET은 REDIS_URL/ADMIN_OPERATOR_*와 달리 dev/prod 둘 다
        # 싣는다(prod 버킷 프로비저닝 완료로 #2887의 「prod엔 키 자체가 없어야 안전」 유보 해소).
        if [ "${_DEPLOY_ENV}" == "prod" ]; then
          ENV_VARS="$${ENV_VARS},GCS_AVATARS_BUCKET=sprintable-avatars-prod"
        else
          ENV_VARS="$${ENV_VARS},GCS_AVATARS_BUCKET=${_GCS_AVATARS_BUCKET}"
        fi
        # story #2141 원칙 재사용(카디르 QA 2026-08-19) — GOTENBERG_SERVICE_URL도 prod엔 키 자체가
        # 없어야 안전(dev 전용 office-converter 하드게이트).
        if [ "${_DEPLOY_ENV}" != "prod" ] && [ -n "${_GOTENBERG_SERVICE_URL}" ]; then
          ENV_VARS="$${ENV_VARS},GOTENBERG_SERVICE_URL=${_GOTENBERG_SERVICE_URL}"
        fi
        # story #2445/#3110/#3118 — DATABASE_URL(PgBouncer, dev/prod 둘 다 sprintable 앱유저)·
        # DATABASE_URL_DIRECT(dev=sprintable 앱유저·prod=postgres 직결)·DATABASE_URL_READ(prod만)·
        # APPLE_PRIVATE_KEY 시크릿 바인딩. dev pgbouncer 경유분은 #3110 2보(userlist에 sprintable
        # 유저 등재+DATABASE_URL_DEV_PGBOUNCER_SPRINTABLE 시크릿 신설, PO 집행)로 sprintable
        # 유저 전환 완료 — dev 서빙이 postgres 수퍼유저 DSN을 쓰는 자리가 이제 0개.
        # story #f2a27d2a(지원v1·1경계) flip PR — SUPPORT_GATEWAY_TOKEN_SECRET을 **dev
        # 분기에만** 추가한다. prod용 시크릿(SUPPORT_GATEWAY_TOKEN_SECRET_PROD)은 아직
        # Secret Manager에 없다(prod 자원 미프로비저닝) — prod SECRETS_FLAG에 넣으면 이
        # 이미 live인 backend prod 배포 자체가 "secret not found"로 깨진다. backend
        # 코드 쪽은 이미 이 값 미설정 시 fail-closed 503(support_gateway_token.py)이라
        # 시크릿을 아예 안 실어도 안전 — prod가 그 상태다.
        SECRETS_FLAG=""
        if [ "${_DEPLOY_ENV}" == "dev" ]; then
          SECRETS_FLAG="--update-secrets=DATABASE_URL=DATABASE_URL_DEV_PGBOUNCER_SPRINTABLE:latest,DATABASE_URL_DIRECT=DATABASE_URL_DIRECT_DEV_SPRINTABLE:latest,APPLE_PRIVATE_KEY=APPLE_SIWA_PRIVATE_KEY:latest,SUPPORT_GATEWAY_TOKEN_SECRET=SUPPORT_GATEWAY_TOKEN_SECRET_dev:latest"
        elif [ "${_DEPLOY_ENV}" == "prod" ]; then
          SECRETS_FLAG="--update-secrets=DATABASE_URL=DATABASE_URL_PROD_PGBOUNCER:latest,DATABASE_URL_DIRECT=DATABASE_URL_PROD:latest,DATABASE_URL_READ=DATABASE_URL_READ:latest,APPLE_PRIVATE_KEY=APPLE_SIWA_PRIVATE_KEY:latest"
        fi
        gcloud run deploy sprintable-backend-${_DEPLOY_ENV} \
          --image=${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:${COMMIT_SHA} \
          --region=${_AR_REGION} \
          --min-instances=${_BACKEND_MIN_INSTANCES} \
          --max-instances=${_BACKEND_MAX_INSTANCES} \
          --timeout=${_BACKEND_TIMEOUT} \
          --allow-unauthenticated \
          --update-env-vars="$${ENV_VARS}" \
          $${SECRETS_FLAG} \
          --remove-env-vars=REDIS_CONSUME_ENABLED \
          --quiet
    # story dbda0baf: push-backend → run-migrate-job(위) 로 변경 — 신규 코드가 마이그 안 된
    # 스키마를 상대로 뜨는 걸 막는다(migrate 실패 시 이 스텝 자체가 안 돎).
    waitFor: [run-migrate-job]

  # E-ARCH 1단계(story #2078): sprintable-realtime-${_DEPLOY_ENV} — SSE/LISTEN 전용 서비스,
  # deploy-backend와 동일 이미지·동일 시크릿 상속(--set-secrets 없는 gcloud run deploy
  # 컨벤션 그대로 — 기존 바인딩 preserve). ⚠️ prod는 PO 검증 전이라 게이트: _DEPLOY_ENV가
  # dev가 아니면 스킵(서비스 미존재 상태에서 이 스텝이 자동 생성해버리는 것 방지 — 프로덕션
  # 승격 시점에 이 가드를 열고 _REALTIME_* 값을 prod로 배선).
  - id: deploy-realtime
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_DEPLOY_ENV}" != "dev" ]; then
          echo "skip deploy-realtime: prod gate pending (E-ARCH 1단계 dev 검증 후 오픈)"
          exit 0
        fi
        # ⛔fail-fast (오르테가군 PO 2026-07-23): `_REALTIME_URL` 기본값을 dev URL 에서 빈
        # 문자열로 내렸다(위 substitutions 주석 — prod 프론트가 dev realtime 을 가리킬 여지 제거).
        # frontend 쪽은 빈 값이 곧 "FASTAPI_URL 로 폴백"이라 안전하지만, **여기서는 빈 값이
        # 안전하지 않다** — 아래 `FASTAPI_URL=${_REALTIME_URL}` 가 빈 문자열로 나가면
        # realtime-dev 의 self URL 이 지워진다. 조용히 깨지는 대신 여기서 멈춘다.
        # 자동 경로(GHA dev 분기)는 항상 명시값을 넘기므로 이 가드에 걸릴 일이 없다 —
        # 걸린다면 substitution 없이 수동 `gcloud builds submit` 한 경우이고, 그건 정말로
        # 멈춰야 하는 상황이다.
        if [ -z "${_REALTIME_URL}" ]; then
          echo "FAIL: _REALTIME_URL 이 비어 있다. deploy-realtime 은 이 값을 realtime 서비스의"
          echo "      FASTAPI_URL 로 쓰므로 빈 값으로 진행하면 self URL 이 지워진다."
          echo "      --substitutions 에 _REALTIME_URL=<realtime 서비스 URL> 을 명시할 것."
          exit 1
        fi
        # story #3079(2026-08-25) 근본수정 — #3031이 여기 심었던 git-diff 기반 path-filter는
        # Cloud Build 워크스페이스에 `.git`이 없어(로컬소스 tarball 업로드, `.gcloudignore`
        # 미비로 gcloud 기본 규칙이 `.git` 항상 제외) 단 한 번도 실제로 스킵하지 못했다 —
        # 매번 "판별 불가" fail-safe로 떨어져 상시 배포였다(실 로그로 확定). 판정을 Cloud
        # Build 밖(GHA, 실 .git 보유 — cloud-build.yml "Resolve realtime deploy path-filter"
        # 스텝)으로 옮기고 결과만 이 substitution으로 받는다.
        if [ "${_REALTIME_CLOUDRUN_SKIP}" = "true" ]; then
          echo "skip deploy-realtime: GHA path-filter(story #3079)가 realtime-관련 경로 변경 없음으로 판정"
          exit 0
        fi
        # ⚠️story #2078 긴급수정(2026-07-21): PG_LISTEN_ENABLED가 여기 true로 하드코딩돼
        # 있었다 — PO가 라이브에서 손으로 false로 전환(LISTEN 제거·Redis 단독 dispatch 확認
        # 완료)했는데, 이 스텝이 명시적으로 true를 다시 밀어넣어 **다음 배포마다 LISTEN이
        # 부활**하는 시한폭탄이었다("손 값은 배포가 덮는다" 규율의 역방향 재발 — 이번엔 배포가
        # 고침을 지우는 쪽). realtime은 이제 LISTEN이 구조적으로 불필요(Redis가 authoritative
        # dispatch) — false로 durable 고정. EVENT_BROKER_REDIS_CONSUME_ENABLED=true도 명시.
        # ⚠️#2123 정정(2026-07-23): "api는 구독 불필요"라 여기 적었었으나 틀렸다 — 에이전트
        # SSE가 backend-dev를 직접 서빙해(agent_onboarding_config.py 설계) api도 이제
        # consume+dispatch=true다(위 deploy-backend 스텝 참조). 브라우저(여기, realtime-dev)와
        # 에이전트(backend-dev)가 서로 다른 프로세스-로컬 큐만 보므로 중복배달은 없다.
        # ⚠️story #2135(2026-07-23): 이 키 이름이 여태 `REDIS_CONSUME_ENABLED`였다 —
        # Settings 필드(`event_broker_redis_consume_enabled`)와 안 맞아 pydantic이 조용히
        # 무시했다(여기서는 필드 기본값이 우연히 True라 결과만 의도와 같았음). 실제 필드가
        # 기대하는 키로 정정 — 구 키는 아래 --remove-env-vars로 명시 제거.
        # ⛔story cd10e123 긴급수정(2026-07-21, codex 리서치 검증 중 적발): REDIS_URL·
        # EVENT_BROKER_REDIS_DUAL_PUBLISH_ENABLED·EVENT_BROKER_REDIS_DISPATCH_ENABLED 셋 다
        # 여태 여기 없는 순수 수동 Cloud Run env였다 — realtime은 이미 LISTEN이 꺼져있고
        # Redis가 유일한 dispatch 경로라, 이 서비스가 재배포/재생성될 때 이 셋이 기본값
        # (false/None)으로 되돌아가면 두 전송경로 동시 무효화(실시간 전면정지)가 된다.
        # 이 스텝은 이미 dev 전용 가드(위) 안이라 true를 직접 명시해도 안전(PG_LISTEN_ENABLED=
        # false와 동일 컨벤션) — REDIS_URL만 변수(연결문자열은 환경별로 다르므로).
        #
        # story #2178(2026-07-24, 까심 #2158 회귀검증에서 적발 → PO가 전수로 넓힘): deploy-backend
        # 는 12키를 넘기는데 이 스텝은 8키뿐이라 backend·realtime 두 서비스가 다른 플래그 세트로
        # 돌고 있었다 — 그 차이 자체가 어디에도 선언된 적이 없어 "켰다"고 믿는 사고가 반복됐다
        # (#2158 SSE_TRANSIENT_REPLAY_ENABLED가 정확히 그 사고: record()는 backend-dev에서 돌아
        # 정상이었는데, 실제 브라우저 SSE를 서빙하는 건 이 realtime-dev라 replay()가 이 서비스
        # 에서 한 번도 안 돌고 있었다). 5종 전수 판정(코드 경로 실증, 값보다 "왜 다른지" 선언이
        # 본체):
        #   ⭐SSE_TRANSIENT_REPLAY_ENABLED  진짜 누락 — replay 대상 연결이 실제로 여기(realtime-
        #     dev의 agent_event_stream)에 있다. 즉시 배선(아래).
        #   ⭐SSE_LEASE_REDIS_ENABLED       진짜 누락 — events.py의 sse_lease.acquire/refresh/
        #     release가 바로 이 파일의 agent_event_stream 안에서 직접 호출된다(연결 시점·30초
        #     하트비트마다·해제 시). realtime-dev가 다인스턴스(_REALTIME_MIN/MAX_INSTANCES=2~8)
        #     라 이게 꺼져 있으면 #2121이 고치려던 전역 429/503 캡이 인스턴스별로 쪼개져 그 결함이
        #     이 서비스에서 그대로 재현된다. 즉시 배선(아래).
        #   PRESENCE_REDIS_ENABLED         의도적 off(무해) — chat_presence는 이 파일(events.py)
        #     어디에도 import/호출이 없다. 쓰기·읽기 전부 conversations.py/team_presence.py(REST
        #     라우트)에만 있어 backend-dev에서만 실행된다.
        #   PRESENCE_ONLINE_REDIS_ENABLED  의도적 off(무해) — 30초 SSE-틱 hot-path 기록은
        #     agent_gateway.py의 /agent/stream(에이전트 전용, backend-dev가 직접 서빙)에만 있다.
        #     이 파일의 agent_event_stream(브라우저 전용)엔 presence_online 참조가 전혀 없다.
        #   FANOUT_WAKE_REDIS_ENABLED      의도적 off(무해) — wake_agent()는 REST 뮤테이션
        #     라우트에서만 호출되고, 대상 큐(_agent_connections)도 에이전트 키인데 에이전트는
        #     backend-dev에만 붙는다 — realtime-dev엔 그 큐 자체가 존재한 적이 없다.
        # 위 두 건(누락)만 배선하고 나머지 셋은 의도적으로 그대로 둔다 — 한 덩어리로 "다 켜자"
        # 하지 않는다(PO 지시).
        # ⚠️이 판정이 딛고 선 전제(오르테가군 PR 리뷰 지적, 2026-07-24) — 위 세 건의 "의도적
        # off" 판정은 **"realtime-dev에는 브라우저만 붙는다"**는 현재 라우팅 전제 위에 서 있다.
        # 이 서비스는 backend와 **같은 풀 이미지**를 돌리므로 `/agent/stream`(agent_gateway.py)
        # 라우트 자체는 살아 있다 — 지금은 에이전트가 항상 backend-dev로 붙도록 배선돼 있어(
        # agent_onboarding_config.py::resolve_backend_direct_url) 무해할 뿐이다. **그 라우팅이
        # 바뀌어 에이전트가 realtime-dev에도 붙게 되면 이 판정 전체를 재검토할 것** — 오늘 이
        # 사달(#2178)의 근본원인이 정확히 "전제가 어디에도 안 적혀 있었던 것"이라, 판정 근거뿐
        # 아니라 판정이 무너지는 조건까지 여기 남긴다.
        # story #2442(P0) — 이 스텝은 위에서 이미 dev 아니면 skip 하므로 prod 값(20/10)은
        # 여기 닿지 않는다(_DEPLOY_ENV=prod면 함수 진입 전에 exit 0). 그래도 하드코딩
        # DB_POOL_SIZE=3,DB_MAX_OVERFLOW=1을 그대로 뒀었다(PO forensic 적발 — 3곳 하드코딩 중
        # 하나) — SSOT 원칙상 "우연히 같은 값"과 "명시로 같은 값"은 다르다. deploy-backend와
        # 동일한 substitution으로 교체(dev 기본값 3/1 그대로 — 값 변경 아님, 배선만 정정).
        gcloud run deploy sprintable-realtime-${_DEPLOY_ENV} \
          --image=${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:${COMMIT_SHA} \
          --region=${_AR_REGION} \
          --min-instances=${_REALTIME_MIN_INSTANCES} \
          --max-instances=${_REALTIME_MAX_INSTANCES} \
          --timeout=${_REALTIME_TIMEOUT} \
          --allow-unauthenticated \
          --update-env-vars=FASTAPI_URL=${_REALTIME_URL},DB_POOL_SIZE=${_DB_POOL_SIZE},DB_MAX_OVERFLOW=${_DB_MAX_OVERFLOW},PG_LISTEN_ENABLED=false,EVENT_BROKER_REDIS_CONSUME_ENABLED=true,REDIS_URL=${_REDIS_URL},EVENT_BROKER_REDIS_DUAL_PUBLISH_ENABLED=true,EVENT_BROKER_REDIS_DISPATCH_ENABLED=true,SSE_TRANSIENT_REPLAY_ENABLED=true,SSE_LEASE_REDIS_ENABLED=true \
          --remove-env-vars=REDIS_CONSUME_ENABLED \
          --quiet
    waitFor: [run-migrate-job]

  # story #2426(2026-08-02) — MCP Cloud Run 서비스를 배포 대상에 넣는다.
  # ⛔발견 경위: `sprintable-mcp-dev` 가 2026-07-29 코드(4일 전), `sprintable-mcp-prod` 가
  # 2026-07-28 코드(main HEAD 대비 449 커밋 前)로 돌고 있었다. 넷이 **같은 backend 이미지**를
  # 쓰도록 만들어진 짝인데, 이 파일이 backend·frontend·realtime 셋만 배포하고 mcp 둘은
  # 목록 밖이었다. `.github` 전체에 `sprintable-mcp-dev` 문자열 0건 — 아무 자동화도 없었다.
  # (`publish-mcp.yml` 은 PyPI 패키지 공개용이고 Cloud Run 과 무관하다. 「MCP 배포」라는 말이
  #  두 가지를 가리켜, 처음엔 그쪽을 원인으로 짚었다가 이미지 태그를 열어 보고 갈렸다.)
  # ⇒ 그래서 오늘 고친 MCP 인자 검증(#2412)이 **정작 그 도구를 쓰는 서버에는 안 닿아 있었다.**
  #
  # ⚠️story #2701(2026-08-17) 후속 — 위 #2426이 「env는 파이프라인 밖(손 설정)」이라 선언했던
  # 그 잔여를 여기서 닫는다. SSOT 위치 = 이 스텝(플레인 값)+아래 AGENT_API_KEY 시크릿
  # (MCP_AGENT_API_KEY_DEV/MCP_AGENT_API_KEY_PROD, Secret Manager) — backend/realtime 스텝과
  # 동일 관례(--update-env-vars additive·--update-secrets, --set-env-vars/--env-vars-file
  # 전체교체 금지 그대로 승계). `services update`→`run deploy`로 바꾸지 않는다(이미지 갱신
  # 방식 자체는 위 #2426 판단 그대로 유효 — env만 추가 배선).
  #
  # AC1 census(2026-08-17, gcloud describe 실측, 값은 기록 안 함— 이름만):
  #   dev  = MCP_TRANSPORT · SPRINTABLE_API_URL · AGENT_API_KEY(시크릿화 대상)
  #   prod = 위 셋 + MCP_ALLOWED_HOSTS(dev엔 없음 — CORS/host allowlist가 아직 dev엔
  #          불필요하다는 뜻으로 실측된 그대로 두고 조용히 안 맞춘다, 위 GHA 주석 참조)
  #
  # ⚠️realtime 과 달리 **prod 게이트를 두지 않는다** — `sprintable-mcp-prod` 는 이미 존재하고
  # 실제로 요청을 받고 있다(2026-08-02 실측). backend-prod 가 이 파이프라인으로 갱신되는데
  # mcp-prod 만 안 따라가는 것이 바로 #2426이 없애려던 «불일치»다.
  - id: deploy-mcp
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        # story #2701: MCP_TRANSPORT·SPRINTABLE_API_URL은 dev/prod 공통 배선(SPRINTABLE_API_URL은
        # 이 파일 다른 스텝의 _FASTAPI_URL과 값 동일 — 새 substitution 안 만들고 재사용).
        # MCP_ALLOWED_HOSTS는 dev에서 빈 문자열(_MCP_ALLOWED_HOSTS 기본값)이면 그 키 자체를
        # 안 싣는다 — 명시로 빈 값을 실으면 "값 없음"과 "빈 문자열"이 앱 입장에서 다른 뜻일 수
        # 있어(예: allowlist 빈 배열=전부 거부 vs 미설정=검증 스킵) 오늘 실측 그대로(dev엔 이
        # 키가 없다)를 보존한다.
        #
        # ⛔fix(디디 교차QA, 2026-08-17) — prod 실값(mcp.sprintable.ai 등 호스트 둘을 콤마로
        # 이은 문자열)이 이 파이프라인을 두 겹으로 지난다: ①GHA→cloudbuild `--substitutions=`
        # 조립(그 자체가 comma-분리 ArgDict) ②아래 `--update-env-vars=`(마찬가지로 comma-분리
        # ArgDict). 값 안의 콤마가 둘 중 어느 층에서든 "다음 key=value의 시작"으로 오인되면
        # CLI 파싱이 죽는다(dev는 이 값이 비어 있어 검증 경로에서 안 드러났던 함정). ①은 GHA
        # output을 `|` 구분자로 나르는 것으로 이미 해소(위 cloud-build.yml 주석 참조·
        # ${_MCP_ALLOWED_HOSTS}는 여기 도달할 때 이미 `|`-구분 상태). ②는 gcloud의 커스텀
        # ArgDict 구분자 이스케이프(`gcloud topic escaping`) — 아래서 `^;^` 접두로 이 플래그의
        # 구분자를 `,`에서 `;`로 바꿔, 값 안의 콤마가 안전한 리터럴이 되게 한다(모든 하위
        # key=value를 `;`로 나열).
        ENV_VARS="MCP_TRANSPORT=http;SPRINTABLE_API_URL=${_FASTAPI_URL}"
        if [ -n "${_MCP_ALLOWED_HOSTS}" ]; then
          MCP_ALLOWED_HOSTS_COMMA=$(printf '%s' "${_MCP_ALLOWED_HOSTS}" | tr '|' ',')
          ENV_VARS="$${ENV_VARS};MCP_ALLOWED_HOSTS=$${MCP_ALLOWED_HOSTS_COMMA}"
        fi
        # AGENT_API_KEY — story #2701(PO 판정, 2026-08-17): plain substitution 금지(cloudbuild.yaml은
        # git 파일 — API 키 평문 커밋 사고 방지). dev/prod 각자 라이브 값을 그대로 Secret Manager로
        # 1회 이관(MCP_AGENT_API_KEY_DEV/_PROD, 키 회전 아님·사본 이동)해 backend deploy-backend
        # 스텝의 DATABASE_URL_* secretKeyRef 관례를 그대로 재사용한다.
        #
        # ⛔hotfix(P0, 2026-08-17) — 첫 실배포(d9f852e7)가 이 스텝에서 죽었다:
        # `ERROR: Cannot update environment variable [AGENT_API_KEY] to the given type because
        # it has already been set with a different type` — AGENT_API_KEY가 이미 **plain env**로
        # 서빙 중인 상태에서 같은 이름을 `--update-secrets`로 시크릿 타입 전환하려 하면 gcloud가
        # 거부한다(타입 충돌, additive 확장이 아니라 같은 키의 종류를 바꾸는 것이라 별개 취급).
        # `--remove-env-vars`로 plain 쪽을 먼저 걷어내고 `--update-secrets`로 시크릿을 얹는 걸
        # **같은 gcloud 호출**에 조합 — 이러면 새 revision 하나로 원자 전환되고(순단 없음), plain
        # 값이 잔존하는 창도 없다. 이후 배포부터는 AGENT_API_KEY가 이미 시크릿이라 remove가
        # no-op(이미 없는 env var 제거 시도 — gcloud는 이를 에러로 안 침, idempotent).
        if [ "${_DEPLOY_ENV}" == "prod" ]; then
          AGENT_KEY_SECRET="MCP_AGENT_API_KEY_PROD"
        else
          AGENT_KEY_SECRET="MCP_AGENT_API_KEY_DEV"
        fi
        gcloud run services update sprintable-mcp-${_DEPLOY_ENV} \
          --image=${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:${COMMIT_SHA} \
          --region=${_AR_REGION} \
          --update-env-vars="^;^$${ENV_VARS}" \
          --remove-env-vars=AGENT_API_KEY \
          --update-secrets="AGENT_API_KEY=$${AGENT_KEY_SECRET}:latest" \
          --quiet
        # 검산: 「배포 success」가 아니라 «서빙 이미지가 backend 와 같은 태그인가»로 잰다.
        served=$(gcloud run services describe sprintable-mcp-${_DEPLOY_ENV} \
          --region=${_AR_REGION} --format='value(spec.template.spec.containers[0].image)')
        expected="${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/backend:${COMMIT_SHA}"
        if [ "$served" != "$expected" ]; then
          echo "FAIL: mcp-${_DEPLOY_ENV} 서빙 이미지가 기대와 다르다"
          echo "  서빙: $served"
          echo "  기대: $expected"
          exit 1
        fi
        # story #2701 AC3 양성대조 일부: env 이름 목록이 배포 전후 동일한지(값은 안 찍음).
        names=$(gcloud run services describe sprintable-mcp-${_DEPLOY_ENV} \
          --region=${_AR_REGION} --format='value(spec.template.spec.containers[0].env[].name)')
        echo "OK: sprintable-mcp-${_DEPLOY_ENV} = backend:${COMMIT_SHA}"
        echo "OK: sprintable-mcp-${_DEPLOY_ENV} env names = $names"
    waitFor: [run-migrate-job]

  # story #3259(지원v1·1경계) — Support Gateway 배포. 하드게이트 기본 off(README.md "프로비저닝
  # (PO 레인)" 참고) — 전용 서비스계정·전용 DB·시크릿이 실제로 만들어지기 전엔 이 스텝이
  # 돌면 존재하지 않는 자원을 가리키다 실패하거나(안전) 최악엔 fleet 서비스계정으로
  # 잘못 뜨는 사고를 만들 수 있어 명시적 opt-in만 허용한다. --update-secrets는 backend 스텝의
  # DATABASE_URL_*/JWT_SECRET과 이름이 겹치지 않는 전용 시크릿만 참조 — fleet 시크릿을
  # 이 서비스가 상속(preserve)할 수 있는 `gcloud run deploy`의 "미지정=기존값 유지" 함정
  # (위 backend 스텝 주석 참고)을 피하려 매 배포마다 --update-secrets를 명시 지정한다.
  #
  # story #3264(지원v1·6방어·계측) — SUPPORT_GATEWAY_ADMIN_TOKEN 추가(어드민 계측 조회 전용
  # 정적 시크릿, app/token_verify.py::require_admin). 나머지 두 시크릿과 동일 패턴(fail-closed
  # 하드 참조) — SUPPORT_GATEWAY_ADMIN_TOKEN_${_DEPLOY_ENV} 시크릿 버전이 없으면 이 배포
  # 스텝 자체가 실패한다(안전측 — 빈 값으로 조용히 뜨는 게 아니다). 페드루 PO가 실물 값을
  # 프로비저닝하기 전까지는 이 스텝이 그 이유로 막힌다 — 코드/배선은 여기까지, 시크릿 생성은
  # PO 레인(infra/cloudbuild-secret-manifest.txt에 SUPPORT_GATEWAY_ADMIN_TOKEN_dev 등재 済).
  - id: deploy-support-gateway
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_SUPPORT_GATEWAY_DEPLOY_ENABLED}" != "true" ]; then
          echo "skip deploy-support-gateway: _SUPPORT_GATEWAY_DEPLOY_ENABLED != true (story #3259, PO 프로비저닝 대기)"
          exit 0
        fi
        # story #b96151da(voided 대체) 수선 — 페드루 PO 실측(gcloud describe diff): migrate
        # job과 동일 사유로 서비스도 VPC egress 없이는 Cloud SQL Private IP에 "dial tcp
        # 10.110.0.3:3307: i/o timeout"으로 죽는다(--add-cloudsql-instances 소켓만으론 부족).
        #
        # story #3261(오케스트레이션) — SUPPORT_GATEWAY_VERTEX_PROJECT는 비밀 아님(프로젝트
        # ID 문자열)이라 시크릿이 아니라 env var로. ⚠️PO 레인 팔로우업: 이 서비스의 전용
        # SA(${_SUPPORT_GATEWAY_SERVICE_ACCOUNT})에 `roles/aiplatform.user` IAM 바인딩이
        # 아직 없으면 Vertex 호출이 403으로 죽는다 — 코드/배포 스텝은 여기까지만 하고 그
        # IAM 부여는 backend/support-gateway 최초 프로비저닝과 동형으로 PO가 gcloud로 한다.
        # story #3260 — CORS_ORIGINS 값(콤마 구분 origin 목록)이 이 --update-env-vars 자체의
        # ArgDict 구분자(,)와 충돌한다(cloudbuild.yaml MCP_ALLOWED_HOSTS 선례와 동일 함정) —
        # `^;^` 커스텀 구분자 이스케이프로 이 플래그의 구분자를 ,에서 ;로 바꾼다(gcloud topic
        # escaping). GHA는 `|`-구분으로 여기까지 값을 나르므로(위 GHA 주석) 먼저 ,로 되돌린다.
        # story #3263(지원v1·5에스컬레이션) — escalation_task가 사람 전달 이벤트를 던지는
        # backend 착지점. 별도 substitution 발명 없이 이미 있는 ${_FASTAPI_URL}(backend
        # 자기 base URL, deploy-backend가 같은 값을 씀)에 고정 경로만 붙인다 — 두 서비스가
        # 같은 backend를 가리키는 값이 서로 다른 SSOT로 갈라지는 함정을 원천 차단.
        ENV_VARS="SUPPORT_GATEWAY_VERTEX_PROJECT=${PROJECT_ID};SUPPORT_GATEWAY_BACKEND_ESCALATION_EVENTS_URL=${_FASTAPI_URL}/api/v2/support/escalation-events"
        if [ -n "${_SUPPORT_GATEWAY_CORS_ORIGINS}" ]; then
          CORS_ORIGINS_COMMA=$(printf '%s' "${_SUPPORT_GATEWAY_CORS_ORIGINS}" | tr '|' ',')
          ENV_VARS="$${ENV_VARS};SUPPORT_GATEWAY_CORS_ORIGINS=$${CORS_ORIGINS_COMMA}"
        fi
        gcloud run deploy support-gateway-${_DEPLOY_ENV} \
          --image=${_AR_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/support-gateway:${COMMIT_SHA} \
          --region=${_AR_REGION} \
          --service-account=${_SUPPORT_GATEWAY_SERVICE_ACCOUNT} \
          --min-instances=${_SUPPORT_GATEWAY_MIN_INSTANCES} \
          --max-instances=${_SUPPORT_GATEWAY_MAX_INSTANCES} \
          --allow-unauthenticated \
          --add-cloudsql-instances=${PROJECT_ID}:${_AR_REGION}:sprintable-${_DEPLOY_ENV} \
          --network=default \
          --subnet=default \
          --vpc-egress=private-ranges-only \
          --update-env-vars="^;^$${ENV_VARS}" \
          --update-secrets="SUPPORT_GATEWAY_DATABASE_URL=SUPPORT_GATEWAY_DATABASE_URL_${_DEPLOY_ENV}:latest,SUPPORT_GATEWAY_TOKEN_SECRET=SUPPORT_GATEWAY_TOKEN_SECRET_${_DEPLOY_ENV}:latest,SUPPORT_GATEWAY_ADMIN_TOKEN=SUPPORT_GATEWAY_ADMIN_TOKEN_${_DEPLOY_ENV}:latest" \
          --quiet
    waitFor: [run-support-gateway-migrate-job]

  # story #2185(2026-07-27) — GCE MIG(realtime-gateway) 자동배포. deploy-realtime(위, Cloud
  # Run 폴백)과 별개 타깃이다. ⛔dev 전용 하드게이트 — prod GCE 는 실사용 중(48h 정상응답
  # 353건)이라 승인 없는 자동 재배포가 되면 안 된다. prod 를 붙이는 시점엔 사람이 이 가드를
  # 코드로 직접 고칠 것(위 deploy-realtime 의 _DEPLOY_ENV 가드와 동일 컨벤션 — env/substitution
  # 으로 여는 방식은 쓰지 않는다). 스크립트 자체가 이미 무중단 rolling-update(--max-unavailable=0
  # --max-surge=1)로 설계돼 있다(story #2114 S5 실증).
  - id: deploy-realtime-gce
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        if [ "${_DEPLOY_ENV}" != "dev" ]; then
          echo "skip deploy-realtime-gce: dev 전용 하드게이트(story #2185) — prod GCE는 사람이 직접 배포한다"
          exit 0
        fi

        # story #2089(원안, 페드루 PO 판정 2026-08-17) — realtime path-filter.
        # ⛔story #3079(2026-08-25) 근본수정 — 여기 있던 git-diff 기반 판정은 Cloud Build
        # 워크스페이스에 `.git`이 없어(로컬소스 tarball 업로드, `.gcloudignore` 미비로
        # gcloud 기본 규칙이 `.git` 항상 제외) 단 한 번도 실제로 스킵하지 못했다 — 매번
        # "판별 불가" fail-safe로 떨어져 상시 배포였다(실 로그로 확定: `git diff ... 실패
        # rc=129: Not a git repository`). 판정을 Cloud Build 밖(GHA, 실 .git 보유 —
        # cloud-build.yml "Resolve realtime deploy path-filter" 스텝)으로 옮기고 결과만
        # 이 substitution으로 받는다(SSOT 이전 — 로직 이중선언 아님, check_realtime_
        # relevant_diff.sh 자체는 그 스텝에서 그대로 재사용).
        if [ "${_REALTIME_GCE_SKIP}" = "true" ]; then
          echo "skip deploy-realtime-gce: GHA path-filter(story #3079)가 realtime-관련 경로 변경 없음으로 판정"
          exit 0
        fi

        GCP_PROJECT="${PROJECT_ID}" GCP_REGION="${_AR_REGION}" COMMIT_SHA="${COMMIT_SHA}" \
          bash backend/scripts/deploy_realtime_gce.sh dev
    waitFor: [run-migrate-job]

options:
  machineType: E2_HIGHCPU_8
  logging: CLOUD_LOGGING_ONLY
