{
  "_comment": [
    "story #2420 AC7 grandfather baseline - 2026-08-02 이 가드 첫 도입 시점 develop의 기존 자리.",
    "유나(수동 스윕) 112건 vs 이 스크립트(같은 문자열/템플릿 리터럴 안 페어링 기준) 90건 - 차이는",
    "PR 본문에 진단 남김. 판정선은 안 흔들었다 - #2093 query_sentinel 선례처럼 숫자가 다르면",
    "'무엇을 다르게 셌는가'를 먼저 보고 닫은 사례.",
    "재확認(PO 지적, 2026-08-02) - warning은 정확히 일치(13=13)했는데 destructive만 거의 두",
    "배(56 vs 31) 갈렸다. '알파 유틸(bg-destructive/N)을 아예 안 세는 것 아닌가'를 확認했으나",
    "아니다 - 31건 중 24건이 알파 기반이다(tint 기반 8건). 진짜 원인은 destructive가 인터랙티브",
    "버튼류(rest/hover/focus가 서로 다른 문자열 인자로 쪼개지는 cn() 패턴)에 알파 유틸을 압도적",
    "으로 많이 써서, '같은 리터럴 안에서만 페어링'하는 이 스캐너가 유독 destructive에서 많이",
    "놓친다는 것이다(paired 24건 vs repo 전체 bg-destructive/N 발생 67건). warning은 거의 전부",
    "배지류 단일 리터럴이라 이 한계의 영향을 거의 안 받는다(paired 3건 vs 전체 9건). 즉",
    "'알파 유틸 미탐지'가 아니라 '분리 리터럴 미탐지가 계열마다 고르지 않게(특히 destructive에",
    "크게) 작동한다'가 결론 - verify-no-new-tint-color-text.ts 상단 주석 한계 ①에도 기록.",
    "⚠️PO 지적(2026-08-02) - 자가 계열마다 '고르지 않게' 놓치므로, 위 90건의 계열별 분포",
    "(destructive 31 · info 25 · success 21 · warning 13)를 '실제 분포'로 읽으면 안 된다 -",
    "그 수는 이 자의 편향을 담고 있다. '늘어나면 빨간불'은 같은 자로 매번 재므로 그대로",
    "유효하지만(편향이 상수), AC4(사용처를 실제로 고치는 판)에서 이 수를 '계열별로 몇 건",
    "고쳐야 하는가'로 읽으면 틀린다 - AC4는 이 baseline이 아니라 전수 스윕으로 대상을",
    "정해야 한다.",
    "AC4(90건 개별 수리)는 이 스토리가 아니다 - 여기 있는 항목은 실패시키지 않는다.",
    "형식: 파일::계열::리터럴내용 (키에 violationKey()와 동일 함수를 통해서만 생성/비교한다).",
    "",
    "story #2575 AC2(2026-08-11) - bg 패턴에 -bg 추가로 새로 잡힌 app/invite/accept/",
    "invite-accept-client.tsx:87 success/destructive 2건을 당시 grandfather로 등재(#2575",
    "이전부터 실재하던 코드). PO 확認: \"패턴만\" 아니라 실 라이트 AA 위반(4.204/4.304, 4.5",
    "미달)이었다 - 추적 노트를 baseline에 남기고 story #2576으로 수리를 예약했다.",
    "",
    "story #2576(2026-08-11) 해소 - 위 2건을 text-foreground로 수리(AA 라이트 18.35/17.95·",
    "다크 16.47/16.72로 여유있게 통과) 후 baseline에서 제거. grandfather는 \"추적된 대기\"였고",
    "이번에 실제로 닫혔다 - #2575 가드가 자기 손으로 드러낸 위반을, 그 가드로 해소 확認까지",
    "한 사례.",
    "",
    "story #2590/#2987(2026-08-12) 재생성 — #2987(TIER1 warning18+TIER3 same-literal)이 계열색",
    "글자를 text-foreground로 이행하며 위반 리터럴이 사라져 baseline 키가 stale해졌다. head",
    "73b3ba22 기준으로 재생성(71→29키). 제거 44 = badge 3키(#2947로 이미 stale) + #2987이",
    "고친 41(info14·warning13·success11·destructive6 중 badge제외분). ⚠️신규로 «보이는» 2건은",
    "실 회귀가 아니라 리터럴-키 re-key 아티팩트다: (a) chat-list-view.tsx info 아바타",
    "(bg-info/15 text-info)는 develop과 동일·미르코 무변경·info 아이콘(대비 통과)이라 pre-existing",
    "grandfather였고 리터럴 whitespace 차로 re-key. (b) workflow-line-editor-section.tsx success는",
    "미르코가 hover:text-success→hover:text-foreground로 개선(rest text-success는 ghost버튼=투명·",
    "tint는 hover-only=same-literal 스캐너가 rest/hover 못 가려 flag하던 오탐)해 리터럴이 바뀌어",
    "re-key. 둘 다 새 결함 아님. verify:no-new-tint엔 can-only-shrink 메타가드 없어 wholesale 재생성",
    "안전(유나 확認). 교차-요소 축은 별 가드(#2590 verify-cross-element-tint-text)."
  ],
  "keys": [
    "app/(authenticated)/[ws]/[proj]/retro/[id]/page.tsx::success::border-success bg-success-tint text-success",
    "app/(authenticated)/[ws]/[proj]/sprints/sprints-client.tsx::destructive::flex size-9 shrink-0 items-center justify-center rounded-full bg-destructive/10 text-destructive",
    "app/(authenticated)/settings/page.tsx::destructive::text-destructive hover:text-destructive data-active:text-destructive data-active:bg-destructive/10",
    "components/agents/access-matrix-tab.tsx::success::border-success/40 bg-success-tint text-success hover:bg-success/15",
    "components/agents/access-matrix-tab.tsx::success::inline-flex size-4 items-center justify-center rounded border border-success/40 bg-success-tint text-success",
    "components/cage/stuck-handoff-section.tsx::destructive::bg-destructive/10 text-destructive",
    "components/canvas/concurrency-prompt.tsx::info::rounded-md border border-info/40 px-2 py-0.5 font-semibold text-info hover:bg-info/10",
    "components/chat/chat-input.tsx::info::flex-shrink-0 rounded-md p-1.5 transition-colors disabled:opacity-40 ${\n              attachMenuOpen || pickerOpen\n                ? 'bg-info/10 text-info'\n                : 'text-muted-foreground hover:bg-muted/60 hover:text-foreground'\n            }",
    "components/chat/chat-list-view.tsx::info::flex h-9 w-9 flex-shrink-0 items-center justify-center rounded-full text-sm font-medium ${\n        conv.type === 'dm' ? 'bg-primary/15 text-primary' : 'bg-info/15 text-info'\n      }",
    "components/chat/chat-list-view.tsx::info::flex h-9 w-9 flex-shrink-0 items-center justify-center rounded-full text-sm font-medium ${\n        isAgentConv\n          ? 'bg-warning-tint text-foreground'\n          : conv.type === 'dm'\n            ? 'bg-primary/15 text-primary'\n            : 'bg-info/15 text-info'\n      }",
    "components/docs/doc-editor.tsx::info::flex size-9 items-center justify-center rounded-full bg-info/10 text-info",
    "components/docs/doc-gate-section.tsx::destructive::bg-destructive/10 text-destructive",
    "components/docs/doc-gate-section.tsx::info::bg-info-tint text-info",
    "components/docs/doc-gate-section.tsx::success::bg-success-tint text-success",
    "components/docs/doc-gate-section.tsx::success::h-7 gap-1 text-success hover:bg-success-tint hover:text-success",
    "components/docs/extensions/file-node.tsx::destructive::flex h-[38px] w-[38px] flex-shrink-0 items-center justify-center rounded-md bg-destructive/10 text-destructive",
    "components/hypotheses/hypothesis-gate-badge.tsx::success::h-7 gap-1 text-success hover:bg-success-tint hover:text-success",
    "components/inbox/approvals-queue.tsx::success::h-7 gap-1 text-success hover:bg-success-tint hover:text-success",
    "components/kanban/story-detail-panel.tsx::destructive:: 피커가 붙는다.\n                      참조 코어(chat-input-entity-tokens.ts/use-entity-picker.ts) diff 0. */}\n                  <EntityAwareTextarea\n                    value={descriptionDraft}\n                    onChange={setDescriptionDraft}\n                    onPaste={handlePasteAttach}\n                    projectId={projectId}\n                    placeholder=\"Markdown 형식으로 작성하세요...\"\n                    className=\"flex field-sizing-content min-h-[160px] w-full resize-y rounded-lg border border-input bg-transparent px-2.5 py-2 font-mono text-sm outline-none transition-colors placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring disabled:cursor-not-allowed disabled:opacity-50\"\n                    autoFocus\n                  />\n                  <div className=\"flex gap-2\">\n                    <Button size=\"sm\" onClick={handleSaveDescription} disabled={savingDescription}>\n                      {savingDescription ? t('loading') : t('save')}\n                    </Button>\n                    <Button size=\"sm\" variant=\"ghost\" onClick={() => { setEditingDescription(false); setDescriptionDraft(story.description ?? ''); }}>\n                      {t('cancel')}\n                    </Button>\n                  </div>\n                </div>\n              ) : story.description ? (\n                // 긴급 정정(2026-07-28, PO 검수·#2275) — 본문 전체에 클릭=편집모드 onClick이\n                // 걸려 있으면 안의 링크 등 대화형 요소가 stopPropagation을 각각 붙여야만\n                // 살아남는 구조라 위험하다(#2566은 링크 하나만 증상 패치). 상호작용 규약을\n                // 아예 「편집 진입은 위 ✎ 수정 버튼으로만」으로 좁혀 원천 차단한다 — 본문\n                // 안의 무엇을 눌러도 편집모드가 끼어들 수 없다.\n                // ⛔이 div에 onClick을 다시 붙이지 않는다 — 편집 진입은 «수정 버튼»으로만.\n                // 본문 안의 링크·멘션·체크박스가 자기 일을 해야 하기 때문이다.\n                <div className=\"mt-2\">\n                  <DescriptionViewer description={story.description} references={outgoingRefs} bareNumberTargets={bareNumberTargets} />\n                </div>\n              ) : (\n                <button\n                  type=\"button\"\n                  onClick={() => setEditingDescription(true)}\n                  className=\"mt-2 w-full rounded-md border border-dashed border-border py-3 text-sm text-muted-foreground transition hover:border-primary hover:text-primary\"\n                >\n                  + {t('addDescription')}\n                </button>\n              )}\n            </div>\n\n            {/* Acceptance Criteria — Description 블록 미러 (E-BOARD-UX S3) */}\n            <div>\n              <div className=\"flex items-center justify-between\">\n                <span className=\"text-[10px] font-mono uppercase tracking-wider text-muted-foreground\">{t('acceptanceCriteria')}</span>\n                {!editingAC && (\n                  <button\n                    type=\"button\"\n                    onClick={() => setEditingAC(true)}\n                    className=\"text-xs text-muted-foreground transition hover:text-foreground\"\n                  >\n                    ✎ {t('edit')}\n                  </button>\n                )}\n              </div>\n              {editingAC ? (\n                <div className=\"mt-2 space-y-2\">\n                  {/* story #2264(C-6) AC3: 같은 설정 한 줄 — description과 동일 컴포넌트 재사용. */}\n                  <EntityAwareTextarea\n                    value={acDraft}\n                    onChange={setAcDraft}\n                    projectId={projectId}\n                    placeholder=\"Markdown 형식으로 작성하세요...\"\n                    className=\"flex field-sizing-content min-h-[160px] w-full resize-y rounded-lg border border-input bg-transparent px-2.5 py-2 font-mono text-sm outline-none transition-colors placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring disabled:cursor-not-allowed disabled:opacity-50\"\n                    autoFocus\n                  />\n                  <div className=\"flex gap-2\">\n                    <Button size=\"sm\" onClick={handleSaveAC} disabled={savingAC}>\n                      {savingAC ? t('loading') : t('save')}\n                    </Button>\n                    <Button size=\"sm\" variant=\"ghost\" onClick={() => { setEditingAC(false); setAcDraft(story.acceptance_criteria ?? ''); }}>\n                      {t('cancel')}\n                    </Button>\n                  </div>\n                </div>\n              ) : story.acceptance_criteria ? (\n                // #2275 — description과 동일 처방: 편집 진입은 위 ✎ 수정 버튼으로만.\n                // ⛔이 div에 onClick을 다시 붙이지 않는다 — 본문 안의 링크·멘션·체크박스가\n                // 자기 일을 해야 하기 때문이다.\n                <div className=\"mt-2\">\n                  <DescriptionViewer description={story.acceptance_criteria} references={outgoingRefs} bareNumberTargets={bareNumberTargets} />\n                </div>\n              ) : (\n                <button\n                  type=\"button\"\n                  onClick={() => setEditingAC(true)}\n                  className=\"mt-2 w-full rounded-md border border-dashed border-border py-3 text-sm text-muted-foreground transition hover:border-primary hover:text-primary\"\n                >\n                  + {t('addAcceptanceCriteria')}\n                </button>\n              )}\n            </div>\n\n            {/* story #2315 — description/acceptance_criteria 저장이 참조를 조용히 거를 수\n                있다는 것을 화면이 말한다(#2294와 동일 컴포넌트·동일 규율 — 종류-무관 문구). */}\n            {referenceDropped.length > 0 && (\n              <ReferenceDropNotice dropped={referenceDropped} onDismiss={() => setReferenceDropped([])} />\n            )}\n\n            {/* Attachments — chat-attach 자산 미러 (E-FILE S4) */}\n            <div>\n              <div className=\"flex items-center justify-between\">\n                <span className=\"text-[10px] font-mono uppercase tracking-wider text-muted-foreground\">{t('attachments')}</span>\n                <button\n                  type=\"button\"\n                  onClick={() => attachInputRef.current?.click()}\n                  disabled={uploadingAttachment || (story.attachments?.length ?? 0) >= STORY_ATTACHMENT_LIMIT}\n                  className=\"flex items-center gap-1 text-xs text-muted-foreground transition hover:text-foreground disabled:opacity-40\"\n                >\n                  <Paperclip className=\"size-3\" /> + 추가\n                </button>\n              </div>\n              <input\n                ref={attachInputRef}\n                type=\"file\"\n                multiple\n                className=\"hidden\"\n                accept=\"image/*,.pdf,.txt,.md,.csv\"\n                onChange={(e) => { void handleAttachFiles(Array.from(e.target.files ?? [])); e.target.value = ''; }}\n              />\n              {story.attachments && story.attachments.length > 0 ? (\n                <div className=\"mt-2 flex flex-col gap-1.5\">\n                  {story.attachments.map((att, i) => {\n                    const isImage = att.content_type?.startsWith('image/');\n                    const Icon = getFileIcon(att.content_type);\n                    const label = att.name ?? '첨부파일';\n                    return (\n                      <div key={att.url ?? i} className=\"group relative\">\n                        {/* a54ddc16 B1: 보드 첨부도 auth-gated 서명 라우트 경유(chat과 동일 컴포넌트·3상태). */}\n                        {att.url ? (\n                          isImage ? (\n                            <AttachmentImage storedUrl={att.url} storyId={story.id} alt={label} />\n                          ) : (\n                            <AttachmentFile storedUrl={att.url} storyId={story.id} label={label} Icon={Icon} />\n                          )\n                        ) : null}\n                        <button\n                          type=\"button\"\n                          onClick={() => void handleRemoveAttachment(att.url)}\n                          className=\"absolute right-1 top-1 hidden rounded bg-destructive/20 p-0.5 text-destructive transition group-hover:block hover:bg-destructive/30\"\n                          aria-label=\"첨부 삭제\"\n                        >\n                          <X className=\"size-3\" />\n                        </button>\n                      </div>\n                    );\n                  })}\n                </div>\n              ) : !uploadingAttachment ? (\n                <button\n                  type=\"button\"\n                  onClick={() => attachInputRef.current?.click()}\n                  className=\"mt-2 w-full rounded-md border border-dashed border-border py-3 text-sm text-muted-foreground transition hover:border-primary hover:text-primary\"\n                >\n                  + {t('addAttachment')}\n                </button>\n              ) : null}\n              {uploadingAttachment && (\n                <div className=\"mt-2 flex items-center gap-2 text-xs text-muted-foreground\">\n                  <Loader2 className=\"size-3.5 animate-spin\" /> {t('loading')}\n                </div>\n              )}\n              {/* story #2105 2차 — handleAttachFiles가 재시도 전 setAttachError(false)를 먼저\n                  호출해(위 정의) 매 시도마다 언마운트→리마운트된다. */}\n              {attachError && (\n                <p role=\"alert\" aria-live=\"assertive\" aria-atomic=\"true\" className=\"mt-1 text-xs text-destructive\">첨부 업로드에 실패했습니다. 다시 시도해 주세요.</p>\n              )}\n            </div>\n\n            {/* Labels */}\n            <div>\n              <div className=\"mb-2 flex items-center justify-between\">\n                <div className=\"flex items-center gap-1.5 text-[10px] font-mono uppercase tracking-wider text-muted-foreground\">\n                  <Tag className=\"size-3\" />\n                  <span>Labels</span>\n                </div>\n                <button\n                  type=\"button\"\n                  onClick={() => setShowLabelPicker((v) => !v)}\n                  className=\"rounded px-1.5 py-0.5 text-[10px] text-muted-foreground hover:bg-muted transition-colors\"\n                >\n                  {showLabelPicker ? '닫기' : '+ 추가'}\n                </button>\n              </div>\n\n              {loadingLabels ? (\n                <p className=\"text-xs text-muted-foreground\">{t('loading')}</p>\n              ) : (\n                <>\n                  {storyLabels.length > 0 ? (\n                    <div className=\"mb-2 flex flex-wrap gap-1.5\">\n                      {storyLabels.map((label) => (\n                        <span key={label.itemLabelId} className=\"group relative inline-flex\">\n                          <LabelChip label={label} />\n                          <button\n                            type=\"button\"\n                            onClick={() => void handleDetachLabel(label.itemLabelId)}\n                            className=\"absolute -right-1 -top-1 hidden h-3.5 w-3.5 items-center justify-center rounded-full bg-muted-foreground/20 text-foreground hover:bg-destructive/80 hover:text-destructive-foreground group-hover:flex\"\n                            aria-label={",
    "components/loops/context-pack-panel.tsx::info::rounded-lg border border-info-border bg-info-tint p-2.5 text-xs text-info",
    "components/nav/profile-menu.tsx::destructive::flex items-center gap-2 text-destructive focus:bg-destructive/10 focus:text-destructive",
    "components/settings/workflow-line-editor-section.tsx::success::gap-1 text-success hover:bg-success-tint hover:text-foreground",
    "components/storage/storage-delete-dialog.tsx::destructive::grid size-[30px] shrink-0 place-items-center rounded-full bg-destructive/10 text-destructive",
    "components/storage/storage-source-usage-list.tsx::info::bg-info/15 text-info",
    "components/storage/storage-source-usage-list.tsx::success::bg-success/15 text-success",
    "components/ui/dropdown-menu.tsx::destructive::group/dropdown-menu-item relative flex cursor-default items-center gap-1.5 rounded-md px-1.5 py-1 text-sm outline-hidden select-none focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-inset:pl-7 data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 data-[variant=destructive]:focus:text-destructive dark:data-[variant=destructive]:focus:bg-destructive/20 data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4 data-[variant=destructive]:*:[svg]:text-destructive",
    "lib/storage/format.ts::destructive::bg-destructive/10 text-destructive",
    "lib/storage/format.ts::info::bg-info/15 text-info",
    "lib/storage/format.ts::success::bg-success/15 text-success"
  ]
}
